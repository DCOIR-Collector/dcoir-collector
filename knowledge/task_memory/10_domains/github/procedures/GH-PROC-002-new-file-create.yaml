record_id: GH-PROC-002
domain: github
record_type: procedure
operation: new_file_create
object_type: governed_readable_text
problem_signature: new readable file must be created directly in GitHub
primary_lane: contents_create_file
preconditions:
  - target path does not already exist or overwrite is not required
  - full content is prepared
step_sequence:
  - step: create
    action: create_file
    purpose: create the new file directly on main or target branch
  - step: verify
    action: fetch_file
    purpose: confirm the file landed with the expected content
verification_steps:
  - fetch_file from main on the exact new path
anti_patterns:
  - do not use this as the default lane for existing-file updates
last_validated: 2026-04-01
confidence: high
