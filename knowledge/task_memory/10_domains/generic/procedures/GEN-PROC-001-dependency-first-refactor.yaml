record_id: GEN-PROC-001
domain: generic
record_type: procedure
operation: dependency_first_refactor
problem_signature: structural change risks breaking the active runtime or primary workflow if cut over too early
step_sequence:
  - define new dependency set
  - create new dependency artifacts first
  - verify each dependency exists and is readable
  - cut over the stable entrypoint last
  - refresh downstream authority and workflow records
anti_patterns:
  - do not replace the entrypoint first
  - do not leave authority docs describing the old layout
last_validated: 2026-04-01
confidence: high
