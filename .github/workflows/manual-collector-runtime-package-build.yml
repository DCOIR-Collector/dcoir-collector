# DCOIR WORKFLOW OPERATOR NOTES
# Workflow key:
# - workflow-build_package-manual-collector-runtime-package-build-0012
# Purpose:
# - Build the standard DCOIR Collector runtime package from repo source.
# Use when:
# - Use before collector runtime release/delivery or when packaging behavior needs manual verification.
# Do not use when:
# - Do not use for Gemini bundle builds or optional EXE builds.
# Trigger model:
# - workflow_dispatch inputs: package_version_override
# Required inputs:
# - package_version_override: Optional package version override (for example 2_0_1) default=
# Required secrets/config:
# - Use secret/config names only as defined in this workflow body and Local Configuration Registry; never place secret values in workflow comments, requests, reports, logs, or bundles.
# Runtime dependencies:
# - Uses project_sources/collector/tools/build_dcoir_collector_runtime_package.py and related validation/restore tooling.
# Expected outputs/readback:
# - Collector delivery package artifacts and repo-workflows report.
# Safety notes:
# - Package validity requires artifact inspection and downstream validation, not workflow completion alone.
# Maintenance notes:
# - Keep this header aligned with executable behavior whenever triggers, inputs, scripts, reports, artifacts, permissions, or safety checks change.
# - Keep this header aligned with current repo-root GitHub guidance and the active `ircore` operating model when authority or routing language changes.
# Authority routing:
# - GitHub is canonical for workflow/source truth and this header owns workflow-specific execution guidance.
# - Supabase `ircore` owns operational routing, validation rules, reusable lessons, and active state.
# - Use legacy Airtable only when a file explicitly requires a migration or historical lookup.
name: manual-collector-runtime-package-build

on:
  workflow_dispatch:
    inputs:
      package_version_override:
        description: Optional package version override (for example 2_0_1)
        required: false
        default: ''
        type: string

permissions:
  contents: read

concurrency:
  group: manual-collector-runtime-package-build-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build_collector_runtime_package:
    runs-on: windows-latest
    permissions:
      contents: read
    env:
      PACKAGE_VERSION_OVERRIDE: ${{ github.event.inputs.package_version_override }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Validate Python syntax
        shell: pwsh
        run: |
          $pyFiles = git ls-files "*.py"
          if (-not $pyFiles) {
            Write-Host "No tracked Python files found."
            exit 0
          }
          $failed = @()
          foreach ($file in $pyFiles) {
            python -m py_compile $file
            if ($LASTEXITCODE -ne 0) {
              $failed += $file
            }
          }
          if ($failed.Count -gt 0) {
            throw "Python syntax validation failed: $($failed -join ', ')"
          }

      - name: Validate PowerShell syntax
        shell: pwsh
        run: |
          $psFiles = @(
            'project_sources/collector/source/DCOIR_Collector.ps1',
            'project_sources/collector/harness/run_DCOIR_Tests.ps1'
          )
          if (Test-Path -LiteralPath 'project_sources/collector/source/parts') {
            $psFiles += Get-ChildItem -Path 'project_sources/collector/source/parts' -File -Filter '*.ps1' | ForEach-Object { $_.FullName }
          }
          $psFiles = @($psFiles | Where-Object { Test-Path -LiteralPath $_ })
          if (-not $psFiles) {
            throw 'Required collector and harness PowerShell files were not found.'
          }
          $parseFailures = @()
          foreach ($file in $psFiles) {
            $tokens = $null
            $parseErrors = $null
            [void][System.Management.Automation.Language.Parser]::ParseFile($file, [ref]$tokens, [ref]$parseErrors)
            if ($parseErrors.Count -gt 0) {
              $messages = ($parseErrors | ForEach-Object { $_.Message }) -join ' | '
              $parseFailures += "${file}: $messages"
            }
          }
          if ($parseFailures.Count -gt 0) {
            $parseFailures | ForEach-Object { Write-Error $_ }
            throw 'PowerShell syntax validation failed.'
          }

      - name: Verify collector runtime package surfaces
        shell: pwsh
        run: |
          $required = @(
            'project_sources/governance/control_plane/CP-01_DCOIR_Version_Manifest.txt',
            'project_sources/governance/control_plane/CP-02_DCOIR_Change_Log.txt',
            'project_sources/collector/docs/DOC-12_DCOIR_Collector_Runtime_Packaging_Pipeline_v1_0_0.txt',
            'project_sources/collector/manifests/Collector_Runtime_Package_Manifest.json',
            'project_sources/collector/source/DCOIR_Collector.ps1',
            'project_sources/collector/harness/run_DCOIR_Tests.ps1',
            'project_sources/collector/source/parts',
            'project_sources/collector/source/parts/DCOIR_Collector.01_Core_State_And_Utilities.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.02_Baseline_Collection_And_Reports.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.03A_Enrich_Session_State.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.03B_Enrich_Actions_Review.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.03C_Enrich_Actions_Retrieval.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.04_Quick_Interface_And_Output.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.04B_Feature_Wave_Targeted_Collection.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.04C_Explicit_Event_Window_Overrides.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.04D_Bounded_Parallel_Runtime.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.04E_Diagnostic_Context_Overrides.ps1',
            'project_sources/collector/source/parts/DCOIR_Collector.05_Main_Entry.ps1',
            'project_sources/collector/tools/compile_dcoir_collector_runtime.py',
            'project_sources/collector/tools/validate_dcoir_collector_runtime_package.py',
            'project_sources/collector/tools/build_dcoir_collector_runtime_package.py',
            'project_sources/collector/tools/restore_dcoir_collector_runtime_zip.py'
          )
          $missing = @($required | Where-Object { -not (Test-Path -LiteralPath $_) })
          if ($missing.Count -gt 0) {
            throw "Missing required collector runtime package surfaces: $($missing -join ', ')"
          }

      - name: Create output directory
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path project_sources/validation/out | Out-Null

      - name: Run collector runtime package validation
        shell: pwsh
        run: |
          python project_sources/collector/tools/validate_dcoir_collector_runtime_package.py --source-dir . --output-dir project_sources/validation/out
          if ($LASTEXITCODE -ne 0) {
            throw 'Collector runtime package validation failed.'
          }

      - name: Run collector documentation quality gate
        shell: pwsh
        run: |
          Remove-Item -Recurse -Force project_sources/validation/out_preflight_documentation_quality -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Force -Path project_sources/validation/out_preflight_documentation_quality | Out-Null
          python project_sources/collector/tools/verify_collector_documentation_quality.py --repo-root . --output-json project_sources/validation/out_preflight_documentation_quality/collector_documentation_quality.json --fail-on-missing-file-help --fail-on-undocumented-functions
          if ($LASTEXITCODE -ne 0) {
            throw 'Collector documentation quality gate failed before packaging.'
          }

      - name: Build collector runtime package
        shell: pwsh
        run: |
          $versionOverride = $env:PACKAGE_VERSION_OVERRIDE
          $cmd = @(
            'python',
            'project_sources/collector/tools/build_dcoir_collector_runtime_package.py',
            '--source-dir', '.',
            '--output-dir', 'project_sources/validation/out'
          )
          if (-not [string]::IsNullOrWhiteSpace($versionOverride)) {
            $cmd += @('--version', $versionOverride)
          }
          & $cmd[0] $cmd[1..($cmd.Count - 1)]
          if ($LASTEXITCODE -ne 0) {
            throw 'Collector runtime package build failed.'
          }

      - name: Restore built collector runtime for harness smoke
        shell: pwsh
        run: |
          $deliveryZip = Get-ChildItem -Path project_sources/validation/out -File -Filter 'DCOIR_Collector_Delivery_Package_*.zip' | Sort-Object LastWriteTime -Descending | Select-Object -First 1
          if (-not $deliveryZip) {
            throw 'Collector delivery package zip was not produced.'
          }
          python project_sources/collector/tools/restore_dcoir_collector_runtime_zip.py --delivery-package-zip $deliveryZip.FullName --base-runtime-zip supporting_assets/DCOIR_Collector.zip --output-dir project_sources/validation/out --output-name DCOIR_Collector_runtime_for_harness.zip
          if ($LASTEXITCODE -ne 0) {
            throw 'Collector runtime zip restore bridge failed.'
          }
          $extractDir = 'project_sources/validation/out/runtime_harness'
          Remove-Item -Recurse -Force $extractDir -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Force -Path $extractDir | Out-Null
          Expand-Archive -LiteralPath project_sources/validation/out/DCOIR_Collector_runtime_for_harness.zip -DestinationPath $extractDir -Force
          "HARNESS_RUNTIME_ZIP=$((Resolve-Path project_sources/validation/out/DCOIR_Collector_runtime_for_harness.zip).Path)" | Add-Content -Path $env:GITHUB_ENV
          "HARNESS_COLLECTOR_PATH=$((Resolve-Path project_sources/validation/out/runtime_harness/DCOIR_Collector.ps1).Path)" | Add-Content -Path $env:GITHUB_ENV

      - name: Run collector core smoke on built runtime package
        shell: pwsh
        working-directory: project_sources
        run: |
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\collector\harness\run_DCOIR_Tests.ps1 -CollectorPath $env:HARNESS_COLLECTOR_PATH -Suite Core -MasterZipPath $env:HARNESS_RUNTIME_ZIP
          if ($LASTEXITCODE -ne 0) {
            throw 'Collector runtime package Core smoke failed.'
          }

      - name: Upload collector runtime package artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: manual-collector-runtime-package-build
          path: |
            project_sources/TestResults/**
            project_sources/validation/out/*.zip
            project_sources/validation/out/*report.json
            project_sources/validation/out/compiled_runtime/**
            project_sources/validation/out/runtime_harness/**
            project_sources/validation/out_preflight_documentation_quality/**
          if-no-files-found: warn
          retention-days: 21
