name: repair-failuregates-exe-harness

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: repair-failuregates-exe-harness-${{ github.ref }}
  cancel-in-progress: false

jobs:
  patch_failuregates_exe_harness:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Patch FailureGates expected-failure harness for optional EXE mode
        shell: pwsh
        run: |
          $path = 'project_sources/collector/harness/run_DCOIR_Tests.ps1'
          if (-not (Test-Path -LiteralPath $path)) {
            throw "Harness source not found: $path"
          }

          $text = Get-Content -LiteralPath $path -Raw
          $old = @'
              'BIND_REJECT' {
                if ($exitCode -ne 0 -and [string]::IsNullOrWhiteSpace($collectorReportedStatus) -and @($missingPatterns).Count -eq 0) {
                  $status = 'PASS'
                  $message = 'Observed expected bind-reject behavior.'
                } else {
                  $message = 'Expected bind-reject behavior was not observed.'
                }
              }
          '@
          $new = @'
              'BIND_REJECT' {
                $observedNativeBindReject = $exitCode -ne 0 -and [string]::IsNullOrWhiteSpace($collectorReportedStatus)
                $observedExecutableRuntimeReject = $script:ResolvedCollectorInvocationMode -eq 'Executable' -and $exitCode -ne 0 -and $collectorReportedStatus -eq 'ERROR'
                if (($observedNativeBindReject -or $observedExecutableRuntimeReject) -and @($missingPatterns).Count -eq 0) {
                  $status = 'PASS'
                  if ($observedExecutableRuntimeReject) {
                    $message = 'Observed expected executable runtime reject behavior for bind-reject gate.'
                  } else {
                    $message = 'Observed expected bind-reject behavior.'
                  }
                } else {
                  $message = 'Expected bind-reject behavior was not observed.'
                }
              }
          '@

          if (-not $text.Contains($old)) {
            if ($text.Contains('$observedExecutableRuntimeReject')) {
              Write-Host 'FailureGates optional EXE harness patch is already present.'
              exit 0
            }
            throw 'Expected BIND_REJECT block was not found; refusing ambiguous patch.'
          }

          $patched = $text.Replace($old, $new)
          Set-Content -LiteralPath $path -Value $patched -Encoding UTF8

          $verifyText = Get-Content -LiteralPath $path -Raw
          if (-not $verifyText.Contains('$observedExecutableRuntimeReject')) {
            throw 'Patch verification failed: executable runtime reject guard not present.'
          }
          if (-not $verifyText.Contains('Observed expected executable runtime reject behavior for bind-reject gate.')) {
            throw 'Patch verification failed: executable success message not present.'
          }

      - name: Commit and push repair
        shell: pwsh
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git status --short
          if (-not (git status --short)) {
            Write-Host 'No source changes to commit.'
            exit 0
          }
          git add project_sources/collector/harness/run_DCOIR_Tests.ps1
          git commit -m 'Patch FailureGates optional EXE harness reject handling'
          git push
