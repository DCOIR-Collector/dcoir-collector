name: manual-chatgpt-stage-out-test

on:
  workflow_dispatch:
    inputs:
      request_path:
        description: "Repo-relative request JSON under chatgpt_staging/requests/"
        required: true
        default: "chatgpt_staging/requests/test_request_001.json"
        type: string
      commit_outputs:
        description: "Commit generated out bundle into chatgpt_staging/out/"
        required: true
        default: true
        type: boolean

permissions:
  contents: write

concurrency:
  group: manual-chatgpt-stage-out-test-${{ github.ref }}
  cancel-in-progress: false

jobs:
  stage-out:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate request and create stage-out bundle
        shell: bash
        run: |
          set -euo pipefail
          REQUEST_PATH='${{ inputs.request_path }}'

          if [[ "$REQUEST_PATH" != chatgpt_staging/requests/*.json ]]; then
            echo "Request path must be under chatgpt_staging/requests/ and end in .json" >&2
            exit 1
          fi
          if [[ ! -f "$REQUEST_PATH" ]]; then
            echo "Request file not found: $REQUEST_PATH" >&2
            exit 1
          fi

          REQUEST_ID="$(python -c 'import json,sys; print(json.load(open(sys.argv[1])).get("request_id", ""))' "$REQUEST_PATH")"
          if [[ -z "$REQUEST_ID" ]]; then
            echo "request_id is required" >&2
            exit 1
          fi
          case "$REQUEST_ID" in
            *[!A-Za-z0-9._-]*|'') echo "Unsafe request_id: $REQUEST_ID" >&2; exit 1 ;;
          esac

          OUT_DIR="chatgpt_staging/out/$REQUEST_ID"
          WORK_DIR="chatgpt_staging/work/$REQUEST_ID"
          rm -rf "$OUT_DIR" "$WORK_DIR"
          mkdir -p "$OUT_DIR/files" "$OUT_DIR/chunks" "$WORK_DIR"

          python - "$REQUEST_PATH" "$OUT_DIR" <<'PY'
          import hashlib, json, os, pathlib, shutil, sys, zipfile

          request_path = pathlib.Path(sys.argv[1])
          out_dir = pathlib.Path(sys.argv[2])
          req = json.loads(request_path.read_text(encoding="utf-8"))
          allowed_roots = req.get("allowed_roots") or ["chatgpt_staging/testdata/"]
          paths = req.get("exact_paths") or []
          search_terms = req.get("search_terms") or []

          def safe_rel(path: str) -> bool:
              if path.startswith("/") or ".." in pathlib.PurePosixPath(path).parts:
                  return False
              return any(path == root.rstrip("/") or path.startswith(root) for root in allowed_roots)

          selected = []
          for p in paths:
              if not safe_rel(p):
                  raise SystemExit(f"Unsafe or disallowed path: {p}")
              pp = pathlib.Path(p)
              if not pp.is_file():
                  raise SystemExit(f"Requested file not found: {p}")
              selected.append(p)

          tracked = os.popen("git ls-files").read().splitlines()
          for term in search_terms:
              term_l = term.lower()
              for p in tracked:
                  if term_l in p.lower() and safe_rel(p) and pathlib.Path(p).is_file():
                      selected.append(p)

          selected = sorted(set(selected))
          if not selected:
              raise SystemExit("No files selected")

          manifest = {
              "request_id": req["request_id"],
              "request_path": str(request_path),
              "mode": req.get("mode", "zip_first_with_chunks_fallback"),
              "allowed_roots": allowed_roots,
              "files": []
          }
          md = ["# ChatGPT staging out manifest", "", f"- request_id: `{req['request_id']}`", f"- request_path: `{request_path}`", "", "## Files", ""]

          zip_path = out_dir / "staged_files.zip"
          with zipfile.ZipFile(zip_path, "w", compression=zipfile.ZIP_DEFLATED) as zf:
              for idx, p in enumerate(selected, 1):
                  source = pathlib.Path(p)
                  data = source.read_bytes()
                  sha256 = hashlib.sha256(data).hexdigest()
                  blob_sha = os.popen(f"git ls-files -s -- {json.dumps(p)}").read().split()[1]
                  rel_copy = out_dir / "files" / p
                  rel_copy.parent.mkdir(parents=True, exist_ok=True)
                  shutil.copy2(source, rel_copy)
                  zf.write(source, arcname=p)
                  text = source.read_text(encoding="utf-8", errors="replace")
                  lines = text.splitlines()
                  chunk_size = 200
                  chunks = []
                  for chunk_no, start in enumerate(range(0, len(lines), chunk_size), 1):
                      chunk_name = f"{idx:03d}__{source.name}__chunk{chunk_no:03d}.txt"
                      chunk_path = out_dir / "chunks" / chunk_name
                      header = [
                          f"original_path: {p}",
                          f"blob_sha: {blob_sha}",
                          f"sha256: {sha256}",
                          f"chunk: {chunk_no}",
                          f"line_start: {start+1}",
                          f"line_end: {min(start+chunk_size, len(lines))}",
                          "---"
                      ]
                      chunk_path.write_text("\n".join(header + lines[start:start+chunk_size]) + "\n", encoding="utf-8")
                      chunks.append(str(chunk_path.relative_to(out_dir)))
                  entry = {"path": p, "bytes": len(data), "lines": len(lines), "blob_sha": blob_sha, "sha256": sha256, "chunks": chunks}
                  manifest["files"].append(entry)
                  md.append(f"- `{p}` | bytes={len(data)} | lines={len(lines)} | blob_sha=`{blob_sha}` | sha256=`{sha256}`")

          (out_dir / "manifest.json").write_text(json.dumps(manifest, indent=2) + "\n", encoding="utf-8")
          (out_dir / "manifest.md").write_text("\n".join(md) + "\n", encoding="utf-8")
          PY

          echo "OUT_DIR=$OUT_DIR" >> "$GITHUB_ENV"

      - name: Upload stage-out artifact
        uses: actions/upload-artifact@v4
        with:
          name: chatgpt-stage-out-test
          path: ${{ env.OUT_DIR }}/**
          retention-days: 7

      - name: Commit stage-out bundle
        if: ${{ inputs.commit_outputs }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$OUT_DIR"
          git commit -m "Stage ChatGPT test readback bundle" || exit 0
          git push
