# DCOIR REUSABLE WORKFLOW CONTRACT
# Source entry workflow: manual-full-validation.yml
# Purpose: reusable implementation module for manual-full-validation; entry workflow retains triggers, permissions, concurrency, and operator-visible contract notes.
name: reusable-manual-full-validation

on:
  workflow_call:
    inputs:
      caller_event_name:
        description: Original caller event name so the reusable body can preserve push/workflow_dispatch/workflow_run branching.
        required: false
        type: string
        default: 'workflow_call'
      suite:
        description: Forwarded `suite` input from the entry workflow.
        required: false
        type: string
        default: 'FullRegression'
    outputs: {}
    secrets: {}

permissions:
  contents: read

jobs:
  full_validation:
    uses: ./.github/workflows/reusable-collector-validation.yml
    permissions:
      contents: read
    with:
      workflow_name: manual-full-validation
      surface_profile: manual_full_validation
      validation_prefix: manual_full
      artifact_name: manual-full-validation-${{ inputs.suite || 'FullRegression' }}
      artifact_paths: |
        project_sources/TestResults/**
        project_sources/validation/out_manual_full_gemini/**
        project_sources/validation/out_manual_full_collector/**
        project_sources/validation/out_manual_full_validate_dcoir_run_fixtures/**
        project_sources/validation/out_preflight_documentation_quality/**
        project_sources/validation/out_manual_full_documentation_quality/**
      artifact_retention_days: 21
      report_title: Manual full validation summary
      report_scope: selected deep validation suite
      report_note: includes TestResults, Gemini smoke-build output, collector runtime output, validate_DCOIR_Run fixture output, and documentation-quality output when produced.
      report_claim: The workflow conclusion is the authoritative pass/fail signal. The selected suite bounds any readiness claim; use FullRegression or equivalent evidence for broad readiness.
      run_windows_powershell_5_1_syntax: true
      build_runtime_for_harness: true
      run_harness_suite: true
      harness_suite: ${{ inputs.suite || 'FullRegression' }}
