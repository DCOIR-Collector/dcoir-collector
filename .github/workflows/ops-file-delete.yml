name: Ops file delete request
on:
  workflow_dispatch:
jobs:
  dry_run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python ops/tools/ops_request_guard.py ops/requests/delete_files.json
