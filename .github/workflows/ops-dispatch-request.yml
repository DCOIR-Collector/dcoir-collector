name: Ops dispatch request
on:
  workflow_dispatch:
jobs:
  dry_run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: python ops/tools/ops_request_guard.py ops/requests/dispatch_action.json
