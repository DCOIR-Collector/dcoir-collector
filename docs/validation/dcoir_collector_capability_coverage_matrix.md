# DCOIR Collector Capability Coverage Matrix

This source-maintained matrix is the human review companion for the harness-generated `collector_capability_coverage.json` and `collector_capability_coverage.md` artifacts. It exists to keep #187 from becoming an ad hoc pass/fail judgment: every advertised collector capability should either have strong FullRegression/manual-full-validation evidence, a deliberate synthetic-only boundary, a justified out-of-scope boundary, or a tracked gap.

## Current High-Confidence Coverage

| Capability | Current Evidence | Status |
| --- | --- | --- |
| Core collect output contract | FullRegression `Core` suite: collect contract and attachment budget checks | Covered when current-head FullRegression passes |
| Basic enrich session lifecycle | Core/session suites: start, add, finalize, reuse, and finalized output contract checks | Covered when new harness passes |
| Enrich session lifecycle edge cases | SessionBehavior suite: finalize-without-open rejection, explicit session reuse, force-new session creation, requested-session finalization, finalized-session append rejection, open-session finalize, add-after-finalize new session creation, missing requested session rejection, and cleanup while an enrich session is open | Covered when new harness passes |
| Valid targeted explicit window preservation | Targeted suite exact `WindowStart` / `WindowEnd` assertions in scope of this #187 batch | Covered when new harness passes |
| Invalid targeted window fallback | FailureGates invalid start, invalid end, inverted window, paired validation rows | Covered when current-head FullRegression passes |
| Synthetic chunking byte reconstruction | ChunkingOversizeArtifact and ChunkingReconstructionMetadata suites | Covered when current-head FullRegression passes |
| Production event-text chunk reconstruction | Security, PowerShell Operational, and Task Scheduler Operational deterministic oversize path in scope of this #187 batch | Covered when new harness passes |
| Bounded Tier 2 collection | `Tier2BoundedCollect` suite invokes `-Tier T2 -Hours 1 -MaxEvents 100`, checks standard collect contract, verifies `Tier=T2`, `Hours=1`, and `MaxEvents=100` metadata, and verifies Tier 2 deep-check artifacts in the bundle | Covered when new harness passes |
| Missing package diagnostics and no-state cleanup | FailureGates missing package checked paths, orphan cleanup, plain cleanup preservation of custom RunId-like no-state and state-backed roots, and explicit custom RunId no-state cleanup | Covered when current-head FullRegression passes |
| Durable harness progress evidence | `progress.jsonl` and `progress.txt` generated by harness in scope of this #187 batch | Covered when new harness passes |
| Retained evidence artifacts | Harness `evidence/` copies for response path artifacts/chunks in scope of this #187 batch | Covered when new harness passes |
| Capability coverage artifact | Harness generated JSON and Markdown coverage artifacts in scope of this #187 batch | Covered when new harness passes |
| Operator knowledge source alignment | Existing indexed knowledge pages were updated for session guardrails, bounded Tier 2 validation, and source-backed quick aliases; documentation-quality now rejects misattached function help blocks | Partial until separate doc/readback review receipt is recorded |

## Remaining Deliberate Gaps Before #187 Closure

| Gap | Risk | Recommended Next Test Work |
| --- | --- | --- |
| Full TargetProfile matrix | Medium/high | Add assertions for Generic, ScriptExecution, PersistenceFollowUp, NetworkOnly, and ProcessAndPowerShell profile-specific guidance, focus fields, included categories, and handoff artifacts. |
| Raw EVTX exact event-window inclusion/exclusion | High where deterministic fixtures are possible | Add fixture-backed event records or a deterministic abstraction so tests can prove include/exclude semantics, not just metadata propagation. |
| Knowledge-doc source alignment is not proven by FullRegression | Medium | Keep a separate doc/readback review gate for operator-facing docs and avoid counting runtime FullRegression assertions as proof of knowledge-page completeness. |
| Broader quick alias artifact contracts | Medium | Assert per-alias target echo, artifact class, session inclusion, and bounded no-data diagnostics. |

#187 should close only when these remaining gaps are either implemented with evidence, explicitly deferred into child issues with rationale, or marked out of automated scope with operator-approved boundaries.
