{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "exec-20260624-pr307-nonactionable-commit-first-006",
  "operator_approved": true,
  "approved_at_utc": "2026-06-24T16:33:00Z",
  "shell": "pwsh",
  "timeout_seconds": 1800,
  "artifact_retention_days": 3,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Reuse the reviewed edit script from request 005, commit and push the PR #307 non-actionable runner/selftest edits before branch validation, then return the workflow workspace to main for clean status reporting.",
  "command": "$ErrorActionPreference = 'Stop'\n$branch = 'codex/issue-306-function-reachability-report'\n$runnerPath = 'scripts/openrouter_pr_review_hardened.py'\n$selftestPath = 'scripts/openrouter_pr_review_hardened_selftest.py'\n$sourceRequestPath = 'chatgpt_staging/exec_requests/exec-20260624-pr307-nonactionable-programmatic-005.json'\n\nfunction Invoke-Git([string[]]$GitArgs) {\n  Write-Host \"git $($GitArgs -join ' ')\"\n  $output = & git @GitArgs\n  $code = $LASTEXITCODE\n  if ($output) { $output | Out-Host }\n  if ($code -ne 0) { throw \"git failed with exit $code`: git $($GitArgs -join ' ')\" }\n}\n\nfunction Commit-IfChanged([string]$Path, [string]$Message) {\n  & git diff --quiet -- $Path\n  $code = $LASTEXITCODE\n  if ($code -eq 0) {\n    Write-Host \"No changes to commit for $Path\"\n    return\n  }\n  if ($code -ne 1) { throw \"git diff failed for $Path with exit $code\" }\n  Invoke-Git @('add', '--', $Path)\n  Invoke-Git @('commit', '-m', $Message)\n}\n\n$sourceRequest = Get-Content -Raw -LiteralPath $sourceRequestPath | ConvertFrom-Json\n$scriptMatch = [regex]::Match($sourceRequest.command, \"(?s)@'\\r?\\n(?<script>.*?)\\r?\\n'@ \\| Set-Content\")\nif (-not $scriptMatch.Success) { throw \"Unable to extract reviewed edit script from $sourceRequestPath\" }\n$editScript = $scriptMatch.Groups['script'].Value\n\n$validationFailure = ''\ntry {\n  Invoke-Git @('fetch', '--no-tags', 'origin', \"refs/heads/$branch`:refs/remotes/origin/$branch\")\n  Invoke-Git @('checkout', '-B', $branch, \"origin/$branch\")\n  Invoke-Git @('config', 'user.name', 'github-actions[bot]')\n  Invoke-Git @('config', 'user.email', '41898282+github-actions[bot]@users.noreply.github.com')\n\n  $scriptPath = Join-Path $env:RUNNER_TEMP 'apply_dcoir_review_nonactionable.py'\n  $editScript | Set-Content -LiteralPath $scriptPath -Encoding UTF8\n  python $scriptPath\n  if ($LASTEXITCODE -ne 0) { throw 'non-actionable edit script failed' }\n\n  Invoke-Git @('diff', '--check', '--', $runnerPath, $selftestPath)\n  Commit-IfChanged -Path $runnerPath -Message 'Harden dcoir review non-actionable findings'\n  Commit-IfChanged -Path $selftestPath -Message 'Test dcoir review non-actionable finding filter'\n  Invoke-Git @('status', '--short')\n  Invoke-Git @('push', 'origin', \"HEAD:refs/heads/$branch\")\n  Write-Host 'Pushed branch head:'\n  Invoke-Git @('rev-parse', 'HEAD')\n\n  Write-Host 'Running post-push validation'\n  python -m py_compile $runnerPath $selftestPath\n  if ($LASTEXITCODE -ne 0) { $validationFailure = 'py_compile failed after push' }\n  if (-not $validationFailure) {\n    python $selftestPath\n    if ($LASTEXITCODE -ne 0) { $validationFailure = 'hardened selftest failed after push' }\n  }\n}\nfinally {\n  try {\n    Invoke-Git @('fetch', '--no-tags', 'origin', 'main')\n    Invoke-Git @('checkout', '-B', 'main', 'origin/main')\n    Invoke-Git @('status', '--short')\n  } catch {\n    Write-Warning \"Unable to restore workflow workspace to main: $_\"\n  }\n}\nif ($validationFailure) { throw $validationFailure }\n"
}
