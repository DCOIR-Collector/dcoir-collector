{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "exec-20260615-pr271-symlink-path-policy-010",
  "operator_approved": true,
  "approved_at_utc": "2026-06-15T14:18:00Z",
  "shell": "powershell_5",
  "timeout_seconds": 1800,
  "artifact_retention_days": 3,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Patch PR #271 current head 1405601bc7c18c1ee5c8078924746fa41a6d78ee for the latest external Codex symlink/path-policy findings after correcting the staging-command backslash escaping and explicit git-add pathspec. The command verifies the exact branch head and expected source/test blobs, updates only the PowerShell inventory, assembly parity, custom checks, and focused tests, rejects symlink escapes before hashing or reading inventory/harness/custom-check targets, runs focused tests plus collector-tools discovery and CodeQL workflow validation, commits to the PR branch, and pushes with the bridged GitHub token. The PR remains Draft and no external review request is posted.",
  "command": "$ErrorActionPreference = 'Stop'\nSet-StrictMode -Version 2.0\n\n$repo = [Environment]::GetEnvironmentVariable('DCOIR_REPO_ROOT','Machine')\nif ([string]::IsNullOrWhiteSpace($repo)) { $repo = $env:GITHUB_WORKSPACE }\nif ([string]::IsNullOrWhiteSpace($repo) -or -not (Test-Path -LiteralPath $repo -PathType Container)) { throw 'Unable to resolve repository root.' }\n\n$branch = 'codex/issue-267-powershell-engine-pester-boundary'\n$expectedHead = '1405601bc7c18c1ee5c8078924746fa41a6d78ee'\n$requestId = 'exec-20260615-pr271-symlink-path-policy-010'\n$fetchRefspec = \"+refs/heads/${branch}:refs/remotes/origin/${branch}\"\n$pushRefspec = \"HEAD:refs/heads/${branch}\"\n$tempRoot = $env:RUNNER_TEMP\nif ([string]::IsNullOrWhiteSpace($tempRoot)) { $tempRoot = [IO.Path]::GetTempPath() }\n$worktree = Join-Path $tempRoot $requestId\n$patchScript = Join-Path $tempRoot \"$requestId.py\"\n$expectedBlobs = @{\n    'project_sources/collector/tools/build_powershell_surface_inventory.py' = '5925c9dfd76044ba1af1ac810f94f8743c2d5e30'\n    'project_sources/collector/tools/run_powershell_assembly_parity.py' = 'b402a8bab71980d3e22f2343b73c29b468b3ab39'\n    'project_sources/collector/tools/run_powershell_custom_checks.py' = '9b1a3083d82f3723812ec0ddd9ea568b3dcb7bb8'\n    'project_sources/collector/tools/test_build_powershell_surface_inventory_path_safety.py' = 'e364e3e46486f901e341e44244c807cbf19de989'\n    'project_sources/collector/tools/test_run_powershell_assembly_parity.py' = '9aed56f0747edf24fcba2747d2a89b35d354e16c'\n    'project_sources/collector/tools/test_run_powershell_custom_checks.py' = '9351e4ec263860bc3b3653835b25069c4c2ebc66'\n}\n$expectedPaths = @('project_sources/collector/tools/build_powershell_surface_inventory.py', 'project_sources/collector/tools/run_powershell_assembly_parity.py', 'project_sources/collector/tools/run_powershell_custom_checks.py', 'project_sources/collector/tools/test_build_powershell_surface_inventory_path_safety.py', 'project_sources/collector/tools/test_run_powershell_assembly_parity.py', 'project_sources/collector/tools/test_run_powershell_custom_checks.py')\n$validationResults = New-Object System.Collections.Generic.List[string]\n\nfunction Invoke-Git {\n    param([Parameter(Mandatory=$true)][string]$Cwd, [Parameter(Mandatory=$true)][string[]]$GitArgs)\n    $previousErrorActionPreference = $ErrorActionPreference\n    $output = @()\n    $exitCode = $null\n    try {\n        $ErrorActionPreference = 'Continue'\n        $output = & git -C $Cwd @GitArgs 2>&1\n        $exitCode = $LASTEXITCODE\n    }\n    finally { $ErrorActionPreference = $previousErrorActionPreference }\n    if ($exitCode -ne 0) { throw \"git $($GitArgs -join ' ') failed with exit code $exitCode`n$output\" }\n    return $output\n}\n\nfunction Invoke-NativeCommand {\n    param([Parameter(Mandatory=$true)][string]$FilePath, [Parameter(Mandatory=$true)][string[]]$Arguments, [Parameter(Mandatory=$true)][string]$Description)\n    $previousErrorActionPreference = $ErrorActionPreference\n    $output = @()\n    $exitCode = $null\n    try {\n        $ErrorActionPreference = 'Continue'\n        $output = & $FilePath @Arguments 2>&1\n        $exitCode = $LASTEXITCODE\n    }\n    finally { $ErrorActionPreference = $previousErrorActionPreference }\n    if ($exitCode -ne 0) { throw \"$Description failed with exit code $exitCode`n$output\" }\n    return $output\n}\n\nfunction Get-GitText {\n    param([Parameter(Mandatory=$true)][string]$Cwd, [Parameter(Mandatory=$true)][string[]]$GitArgs)\n    $output = Invoke-Git -Cwd $Cwd -GitArgs $GitArgs\n    return (($output | Out-String).Trim())\n}\n\nfunction Get-TrackedBlob {\n    param([Parameter(Mandatory=$true)][string]$Cwd, [Parameter(Mandatory=$true)][string]$Path)\n    $line = Get-GitText -Cwd $Cwd -GitArgs @('ls-files','-s','--',$Path)\n    if ([string]::IsNullOrWhiteSpace($line)) { return '' }\n    return (($line -split '\\s+')[1])\n}\n\nfunction Get-RequiredGitToken {\n    foreach ($name in @('DCOIR_GITHUB_FG_TOKEN','DCOIR_GITHUB_CL_TOKEN')) {\n        $value = [Environment]::GetEnvironmentVariable($name, 'Process')\n        if ([string]::IsNullOrWhiteSpace($value)) { $value = [Environment]::GetEnvironmentVariable($name, 'Machine') }\n        if (-not [string]::IsNullOrWhiteSpace($value)) {\n            Write-Host \"Using bridged GitHub token secret for PR branch push: $name\"\n            Write-Host \"::add-mask::$value\"\n            return $value\n        }\n    }\n    throw 'No bridged GitHub token secret is available for PR branch push.'\n}\n\nfunction Invoke-GitPushWithToken {\n    param([Parameter(Mandatory=$true)][string]$Cwd, [Parameter(Mandatory=$true)][string]$Token, [Parameter(Mandatory=$true)][string]$Refspec)\n    $repoFull = $env:GITHUB_REPOSITORY\n    if ([string]::IsNullOrWhiteSpace($repoFull)) { $repoFull = 'DCOIR-Collector/dcoir-collector' }\n    $pushUrl = \"https://x-access-token:${Token}@github.com/${repoFull}.git\"\n    $previousErrorActionPreference = $ErrorActionPreference\n    $output = @()\n    $exitCode = $null\n    try {\n        $ErrorActionPreference = 'Continue'\n        $output = & git -C $Cwd -c 'http.https://github.com/.extraheader=' push $pushUrl $Refspec 2>&1\n        $exitCode = $LASTEXITCODE\n    }\n    finally { $ErrorActionPreference = $previousErrorActionPreference }\n    if ($exitCode -ne 0) {\n        $safeOutput = (($output | Out-String).Replace($Token, '[REDACTED:GITHUB_TOKEN]')).Trim()\n        throw \"git push to PR branch failed with exit code $exitCode`n$safeOutput\"\n    }\n    return $output\n}\n\nInvoke-Git -Cwd $repo -GitArgs @('fetch','origin',$fetchRefspec) | Out-Null\n$remoteHead = Get-GitText -Cwd $repo -GitArgs @('rev-parse',\"origin/$branch\")\nif ($remoteHead -ne $expectedHead) { throw \"PR branch head mismatch. Expected $expectedHead but origin/$branch is $remoteHead.\" }\n\nif (Test-Path -LiteralPath $worktree) { Remove-Item -LiteralPath $worktree -Recurse -Force }\nInvoke-Git -Cwd $repo -GitArgs @('worktree','prune') | Out-Null\nInvoke-Git -Cwd $repo -GitArgs @('worktree','add','--detach',$worktree,\"origin/$branch\") | Out-Null\n\ntry {\n    foreach ($path in $expectedPaths) {\n        $actualBlob = Get-TrackedBlob -Cwd $worktree -Path $path\n        if ($actualBlob -ne $expectedBlobs[$path]) { throw \"Blob mismatch before patch for $path. Expected $($expectedBlobs[$path]) but got $actualBlob.\" }\n    }\n    $pythonPatch = @'\n\nfrom pathlib import Path\nimport sys\n\nrepo = Path(sys.argv[1])\nsource_inventory = repo / \"project_sources/collector/tools/build_powershell_surface_inventory.py\"\nsource_assembly = repo / \"project_sources/collector/tools/run_powershell_assembly_parity.py\"\nsource_custom = repo / \"project_sources/collector/tools/run_powershell_custom_checks.py\"\ntest_inventory = repo / \"project_sources/collector/tools/test_build_powershell_surface_inventory_path_safety.py\"\ntest_assembly = repo / \"project_sources/collector/tools/test_run_powershell_assembly_parity.py\"\ntest_custom = repo / \"project_sources/collector/tools/test_run_powershell_custom_checks.py\"\n\n\ndef read(path: Path) -> str:\n    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\").replace(\"\\r\", \"\\n\")\n\n\ndef write(path: Path, text: str) -> None:\n    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n\n\ndef replace_top_level_function(text: str, name: str, replacement: str) -> str:\n    marker = f\"def {name}(\"\n    start = text.find(marker)\n    if start == -1:\n        raise SystemExit(f\"Function not found: {name}\")\n    next_start = text.find(\"\\ndef \", start + 1)\n    if next_start == -1:\n        raise SystemExit(f\"Could not find function boundary after: {name}\")\n    return text[:start] + replacement.rstrip() + \"\\n\\n\" + text[next_start + 1:]\n\n\ninventory_text = read(source_inventory)\nif \"def path_resolves_inside_repo\" not in inventory_text:\n    inventory_text = inventory_text.replace(\n        \"\\ndef file_facts(repo_root: Path, rel: str, exists: bool) -> dict[str, Any]:\\n\",\n        \"\\ndef path_resolves_inside_repo(path: Path, repo_root: Path) -> bool:\\n\"\n        \"    try:\\n\"\n        \"        path.resolve().relative_to(repo_root.resolve())\\n\"\n        \"    except (OSError, ValueError):\\n\"\n        \"        return False\\n\"\n        \"    return True\\n\\n\\n\"\n        \"def file_facts(repo_root: Path, rel: str, exists: bool) -> dict[str, Any]:\\n\",\n        1,\n    )\ninventory_text = replace_top_level_function(\n    inventory_text,\n    \"file_facts\",\n    '''def file_facts(repo_root: Path, rel: str, exists: bool) -> dict[str, Any]:\n    if not exists:\n        return {\n            \"size_bytes\": None,\n            \"line_count\": None,\n            \"sha256\": None,\n        }\n    path = repo_root / rel\n    if not path_resolves_inside_repo(path, repo_root):\n        return {\n            \"size_bytes\": None,\n            \"line_count\": None,\n            \"sha256\": None,\n        }\n    try:\n        data = path.read_bytes()\n    except OSError:\n        return {\n            \"size_bytes\": None,\n            \"line_count\": None,\n            \"sha256\": None,\n        }\n    fact_data = normalized_text_fact_bytes(data)\n    return {\n        \"size_bytes\": len(fact_data),\n        \"line_count\": fact_data.count(b\"\\\\n\") + (1 if fact_data and not fact_data.endswith(b\"\\\\n\") else 0),\n        \"sha256\": hashlib.sha256(fact_data).hexdigest(),\n    }''',\n)\ninventory_text = replace_top_level_function(\n    inventory_text,\n    \"filesystem_files\",\n    '''def filesystem_files(repo_root: Path) -> list[str]:\n    files: list[str] = []\n    for path in repo_root.rglob(\"*\"):\n        if not path.is_file():\n            continue\n        try:\n            rel = path.relative_to(repo_root).as_posix()\n        except (OSError, ValueError):\n            continue\n        if is_ignored_discovery_path(rel):\n            continue\n        files.append(rel)\n    return sorted(files)''',\n)\ninventory_text = replace_top_level_function(\n    inventory_text,\n    \"harness_source_part_paths\",\n    '''def harness_source_part_paths(repo_root: Path) -> list[str]:\n    root = repo_root / HARNESS_PARTS_ROOT\n    if not root.is_dir():\n        return []\n    paths: list[str] = []\n    for path in root.glob(\"*.ps1.txt\"):\n        if not path.is_file():\n            continue\n        try:\n            paths.append(path.relative_to(repo_root).as_posix())\n        except (OSError, ValueError):\n            continue\n    return sorted(paths)''',\n)\nold_validation = '''        if entry[\"source_type\"] not in REQUIRED_SOURCE_TYPES:\n            errors.append(f\"{entry['path']}: unsupported source type {entry['source_type']}\")\n        if (\n            entry[\"source_type\"] != \"workflow_yaml\"\n'''\nnew_validation = '''        if entry[\"source_type\"] not in REQUIRED_SOURCE_TYPES:\n            errors.append(f\"{entry['path']}: unsupported source type {entry['source_type']}\")\n        if entry.get(\"exists\") and entry.get(\"size_bytes\") is None:\n            errors.append(f\"{entry['path']}: file facts could not be collected safely inside the repository root\")\n        if (\n            entry[\"source_type\"] != \"workflow_yaml\"\n'''\nif old_validation not in inventory_text:\n    raise SystemExit(\"inventory validation insertion point not found\")\ninventory_text = inventory_text.replace(old_validation, new_validation, 1)\nwrite(source_inventory, inventory_text)\n\ninventory_test_text = read(test_inventory)\ninventory_symlink_test = r'''    def test_symlinked_inventory_file_fails_closed_before_hashing(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            root = Path(temp).resolve()\n            outside = root.parent / \"outside_inventory.ps1\"\n            outside.write_text('Write-Output \"outside\"\\n', encoding=\"utf-8\")\n            rel = \"operator_tools/linked.ps1\"\n            link = root / rel\n            link.parent.mkdir(parents=True, exist_ok=True)\n            try:\n                link.symlink_to(outside)\n            except (NotImplementedError, OSError) as exc:\n                self.skipTest(f\"symlink creation is unavailable: {exc}\")\n\n            surface = inventory.make_surface(\n                root,\n                rel,\n                \"operator_tooling\",\n                \"tooling\",\n                \"include\",\n                \"test symlink surface\",\n                True,\n            )\n            validation = inventory.validate_inventory(\n                [surface],\n                \"changed\",\n                {},\n                {\"input_paths\": [rel], \"rules\": []},\n            )\n\n        self.assertIsNone(surface[\"sha256\"])\n        self.assertFalse(validation[\"success\"])\n        self.assertTrue(\n            any(\"file facts could not be collected safely inside the repository root\" in error for error in validation[\"errors\"]),\n            validation[\"errors\"],\n        )\n\n'''\nif \"test_symlinked_inventory_file_fails_closed_before_hashing\" not in inventory_test_text:\n    inventory_test_text = inventory_test_text.replace(\n        \"    def test_manifest_paths_preserve_valid_repo_relative_inputs(self) -> None:\\n\",\n        inventory_symlink_test + \"    def test_manifest_paths_preserve_valid_repo_relative_inputs(self) -> None:\\n\",\n        1,\n    )\nwrite(test_inventory, inventory_test_text)\n\nassembly_text = read(source_assembly)\nif \"def path_resolves_inside_repo\" not in assembly_text:\n    assembly_text = assembly_text.replace(\n        \"\\ndef safe_relpath(path: Path, repo_root: Path) -> str:\\n\",\n        \"\\ndef path_resolves_inside_repo(path: Path, repo_root: Path) -> bool:\\n\"\n        \"    try:\\n\"\n        \"        path.resolve().relative_to(repo_root.resolve())\\n\"\n        \"    except (OSError, ValueError):\\n\"\n        \"        return False\\n\"\n        \"    return True\\n\\n\\n\"\n        \"def safe_relpath(path: Path, repo_root: Path) -> str:\\n\",\n        1,\n    )\nassembly_text = replace_top_level_function(\n    assembly_text,\n    \"file_facts\",\n    '''def file_facts(path: Path, repo_root: Path) -> dict[str, Any]:\n    if not path.is_file() or not path_resolves_inside_repo(path, repo_root):\n        return {\n            \"path\": safe_relpath(path, repo_root),\n            \"exists\": False,\n            \"size_bytes\": None,\n            \"line_count\": None,\n            \"sha256\": None,\n        }\n    data = path.read_bytes()\n    return {\n        \"path\": safe_relpath(path, repo_root),\n        \"exists\": True,\n        \"size_bytes\": len(data),\n        \"line_count\": data.count(b\"\\\\n\") + (1 if data and not data.endswith(b\"\\\\n\") else 0),\n        \"sha256\": hashlib.sha256(data).hexdigest(),\n    }''',\n)\nassembly_text = replace_top_level_function(\n    assembly_text,\n    \"harness_part_paths\",\n    '''def harness_part_paths(repo_root: Path, errors: list[str] | None = None) -> list[Path]:\n    root = repo_root / HARNESS_PARTS_ROOT\n    paths: list[Path] = []\n    for path in sorted(root.glob(\"run_DCOIR_Tests.part-*.ps1.txt\")):\n        if not path.is_file():\n            continue\n        if not path_resolves_inside_repo(path, repo_root):\n            if errors is not None:\n                errors.append(f\"{safe_relpath(path, repo_root)}: harness source part must resolve inside the repository root\")\n            continue\n        paths.append(path)\n    return paths''',\n)\nold_harness_call = \"    parts = harness_part_paths(repo_root)\\n\"\nif old_harness_call not in assembly_text:\n    raise SystemExit(\"harness_part_paths call site not found\")\nassembly_text = assembly_text.replace(old_harness_call, \"    parts = harness_part_paths(repo_root, errors)\\n\", 1)\nwrite(source_assembly, assembly_text)\n\nassembly_test_text = read(test_assembly)\nassembly_symlink_test = r'''    def test_symlinked_harness_part_fails_before_part_entry_or_read(self) -> None:\n        with self.make_repo() as temp:\n            root = Path(temp).resolve()\n            outside = root.parent / \"outside_harness_part.ps1.txt\"\n            write(outside, 'function Invoke-OutsideHarness { Write-Output \"outside\" }\\n')\n            harness_link = root / \"project_sources/collector/harness/source/parts/run_DCOIR_Tests.part-000.ps1.txt\"\n            try:\n                harness_link.unlink()\n                harness_link.symlink_to(outside)\n            except (NotImplementedError, OSError) as exc:\n                self.skipTest(f\"symlink creation is unavailable: {exc}\")\n            original_part_entry = parity.part_entry\n            original_read_part_text = parity.read_part_text\n\n            def assert_repo_contained(path: Path) -> None:\n                try:\n                    path.resolve().relative_to(root)\n                except ValueError as exc:\n                    raise AssertionError(f\"unsafe harness source read attempted: {path}\") from exc\n\n            def guarded_part_entry(path: Path, repo_root: Path) -> dict[str, object]:\n                assert_repo_contained(path)\n                return original_part_entry(path, repo_root)\n\n            def guarded_read_part_text(path: Path) -> str:\n                assert_repo_contained(path)\n                return original_read_part_text(path)\n\n            with unittest.mock.patch.object(parity, \"part_entry\", side_effect=guarded_part_entry), unittest.mock.patch.object(parity, \"read_part_text\", side_effect=guarded_read_part_text):\n                report, errors, _warnings = parity.build_report(self.args(root))\n\n        self.assertFalse(report[\"validation\"][\"success\"])\n        self.assertTrue(any(\"harness source part must resolve inside the repository root\" in error for error in errors), errors)\n\n'''\nif \"test_symlinked_harness_part_fails_before_part_entry_or_read\" not in assembly_test_text:\n    assembly_test_text = assembly_test_text.replace(\n        \"    def test_missing_source_output_mapping_fails(self) -> None:\\n\",\n        assembly_symlink_test + \"    def test_missing_source_output_mapping_fails(self) -> None:\\n\",\n        1,\n    )\nwrite(test_assembly, assembly_test_text)\n\ncustom_text = read(source_custom)\ncustom_text = replace_top_level_function(\n    custom_text,\n    \"is_absolute_repo_input\",\n    '''def is_absolute_repo_input(value: str) -> bool:\n    raw = value.strip()\n    slash_path = raw.replace(\"\\\\\\\\\", \"/\")\n    return slash_path.startswith(\"/\") or re.match(r\"^[A-Za-z]:\", slash_path) is not None or Path(raw).is_absolute()''',\n)\nif \"def safe_inventory_path\" not in custom_text:\n    custom_text = custom_text.replace(\n        \"\\ndef line_number_for(text: str, pattern: str, flags: int = re.IGNORECASE | re.MULTILINE) -> int | None:\\n\",\n        \"\\ndef safe_inventory_path(value: str, label: str, repo_root: Path, errors: list[str]) -> str:\\n\"\n        \"    raw = value.strip()\\n\"\n        \"    rel = normalize_repo_path(raw)\\n\"\n        \"    slash_path = raw.replace(\\\"\\\\\\\\\\\", \\\"/\\\")\\n\"\n        \"    raw_parts = tuple(part for part in slash_path.split(\\\"/\\\") if part)\\n\"\n        \"    parts = Path(rel).parts\\n\"\n        \"    if not raw or is_absolute_repo_input(raw) or \\\"..\\\" in raw_parts or rel.startswith(\\\"../\\\") or \\\"..\\\" in parts or Path(rel).is_absolute():\\n\"\n        \"        errors.append(f\\\"{label}: path must be a repo-relative path without traversal\\\")\\n\"\n        \"        return \\\"\\\"\\n\"\n        \"    root = repo_root.resolve()\\n\"\n        \"    try:\\n\"\n        \"        resolved = (root / rel).resolve()\\n\"\n        \"        resolved.relative_to(root)\\n\"\n        \"    except (OSError, ValueError):\\n\"\n        \"        errors.append(f\\\"{label}: path must resolve inside the repository root\\\")\\n\"\n        \"        return \\\"\\\"\\n\"\n        \"    return rel\\n\\n\\n\"\n        \"def line_number_for(text: str, pattern: str, flags: int = re.IGNORECASE | re.MULTILINE) -> int | None:\\n\",\n        1,\n    )\ncustom_text = replace_top_level_function(\n    custom_text,\n    \"validate_inventory\",\n    '''def validate_inventory(inventory: dict[str, Any], repo_root: Path) -> tuple[set[str], list[str], list[str]]:\n    errors: list[str] = []\n    warnings: list[str] = []\n    if inventory.get(\"schema_version\") != INVENTORY_SCHEMA_VERSION:\n        errors.append(\n            f\"PowerShell inventory schema mismatch: expected {INVENTORY_SCHEMA_VERSION}, got {inventory.get('schema_version')!r}\"\n        )\n    validation = inventory.get(\"validation\", {})\n    if validation.get(\"errors\"):\n        errors.extend(f\"inventory validation error: {error}\" for error in validation.get(\"errors\", []))\n    warnings.extend(f\"inventory warning: {warning}\" for warning in validation.get(\"warnings\", []))\n    surfaces = inventory.get(\"surfaces\")\n    if not isinstance(surfaces, list) or not surfaces:\n        errors.append(\"PowerShell inventory must contain surfaces[]\")\n        return set(), errors, warnings\n    surface_paths: set[str] = set()\n    for index, surface in enumerate(surfaces, start=1):\n        if not isinstance(surface, dict):\n            continue\n        raw_path = scalar(surface.get(\"path\")).strip()\n        if raw_path:\n            path = safe_inventory_path(raw_path, f\"inventory surface #{index}\", repo_root, errors)\n            if path:\n                surface_paths.add(path)\n    return surface_paths, errors, warnings''',\n)\ncustom_text = replace_top_level_function(\n    custom_text,\n    \"inventory_targets\",\n    '''def inventory_targets(inventory: dict[str, Any]) -> list[str]:\n    targets: list[str] = []\n    for surface in inventory.get(\"surfaces\", []):\n        if not isinstance(surface, dict):\n            continue\n        if surface.get(\"inclusion_decision\") != \"include\":\n            continue\n        if surface.get(\"source_type\") not in ANALYZABLE_SOURCE_TYPES:\n            continue\n        path = scalar(surface.get(\"path\")).strip()\n        if path:\n            targets.append(normalize_repo_path(path))\n    return sorted(dict.fromkeys(targets))''',\n)\ncustom_text = custom_text.replace(\n    \"        surface_paths, inventory_errors, inventory_warnings = validate_inventory(inventory)\\n\",\n    \"        surface_paths, inventory_errors, inventory_warnings = validate_inventory(inventory, repo_root)\\n\",\n    1,\n)\nold_scan = '''        for target in targets:\n            target_path = repo_root / target\n            if not target_path.is_file():\n                errors.append(f\"selected PowerShell source missing: {target}\")\n                continue\n            text = target_path.read_text(encoding=\"utf-8\", errors=\"ignore\")\n            findings.extend(run_checks_for_text(text, target, check_map))\n'''\nnew_scan = '''        for target in targets:\n            safe_target = safe_inventory_path(target, f\"selected target {target}\", repo_root, errors)\n            if not safe_target:\n                continue\n            target_path = repo_root / safe_target\n            if not target_path.is_file():\n                errors.append(f\"selected PowerShell source missing: {safe_target}\")\n                continue\n            text = target_path.read_text(encoding=\"utf-8\", errors=\"ignore\")\n            findings.extend(run_checks_for_text(text, safe_target, check_map))\n'''\nif old_scan not in custom_text:\n    raise SystemExit(\"custom check target scan block not found\")\ncustom_text = custom_text.replace(old_scan, new_scan, 1)\nwrite(source_custom, custom_text)\n\ncustom_test_text = read(test_custom)\ncustom_inventory_test = r'''    def test_inventory_targets_must_be_safe_before_reading(self) -> None:\n        with self.make_repo() as temp:\n            root = Path(temp).resolve()\n            outside = root.parent / \"outside_custom_check.ps1\"\n            write(outside, '$Rows += [pscustomobject]@{ Check = \"Outside\"; Status = \"FAIL\" }\\n')\n            inventory_path = root / custom.DEFAULT_INVENTORY\n            inventory = json.loads(inventory_path.read_text(encoding=\"utf-8\"))\n            inventory[\"surfaces\"].extend([\n                surface(outside.as_posix()),\n                surface(\"../outside_custom_check.ps1\"),\n                surface(\"C:outside/custom_check.ps1\"),\n            ])\n            inventory_path.write_text(json.dumps(inventory, indent=2) + \"\\n\", encoding=\"utf-8\")\n            report, errors, _warnings = custom.build_report(self.args(root, target_scope=\"inventory\"))\n\n        self.assertFalse(report[\"validation\"][\"success\"])\n        self.assertTrue(any(\"path must be a repo-relative path without traversal\" in error for error in errors), errors)\n\n    def test_symlinked_inventory_target_fails_before_reading(self) -> None:\n        with self.make_repo() as temp:\n            root = Path(temp).resolve()\n            outside = root.parent / \"outside_custom_check_symlink.ps1\"\n            write(outside, '$Rows += [pscustomobject]@{ Check = \"Outside\"; Status = \"FAIL\" }\\n')\n            rel = \"project_sources/collector/fixtures/powershell_analysis/bad/symlink_escape.ps1\"\n            link = root / rel\n            link.parent.mkdir(parents=True, exist_ok=True)\n            try:\n                link.symlink_to(outside)\n            except (NotImplementedError, OSError) as exc:\n                self.skipTest(f\"symlink creation is unavailable: {exc}\")\n            inventory_path = root / custom.DEFAULT_INVENTORY\n            inventory = json.loads(inventory_path.read_text(encoding=\"utf-8\"))\n            inventory[\"surfaces\"].append(surface(rel))\n            inventory_path.write_text(json.dumps(inventory, indent=2) + \"\\n\", encoding=\"utf-8\")\n            report, errors, _warnings = custom.build_report(self.args(root, target_scope=\"inventory\"))\n\n        self.assertFalse(report[\"validation\"][\"success\"])\n        self.assertTrue(any(\"path must resolve inside the repository root\" in error for error in errors), errors)\n\n'''\nif \"test_inventory_targets_must_be_safe_before_reading\" not in custom_test_text:\n    custom_test_text = custom_test_text.replace(\n        \"    def test_real_custom_contract_has_negative_and_control_for_each_check(self) -> None:\\n\",\n        custom_inventory_test + \"    def test_real_custom_contract_has_negative_and_control_for_each_check(self) -> None:\\n\",\n        1,\n    )\nwrite(test_custom, custom_test_text)\n\nfor path, needles in {\n    source_inventory: [\"path_resolves_inside_repo\", \"file facts could not be collected safely inside the repository root\", \"path.relative_to(repo_root).as_posix()\"],\n    test_inventory: [\"test_symlinked_inventory_file_fails_closed_before_hashing\"],\n    source_assembly: [\"harness source part must resolve inside the repository root\", \"parts = harness_part_paths(repo_root, errors)\", \"path_resolves_inside_repo\"],\n    test_assembly: [\"test_symlinked_harness_part_fails_before_part_entry_or_read\", \"guarded_read_part_text\"],\n    source_custom: [\"def safe_inventory_path\", \"validate_inventory(inventory, repo_root)\", \"selected target\", \"re.match(r\\\"^[A-Za-z]:\\\"\"],\n    test_custom: [\"test_inventory_targets_must_be_safe_before_reading\", \"test_symlinked_inventory_target_fails_before_reading\"],\n}.items():\n    text = read(path)\n    for needle in needles:\n        if needle not in text:\n            raise SystemExit(f\"Post-patch sanity check failed for {path}: missing {needle}\")\n\n'@\n    [System.IO.File]::WriteAllText($patchScript, $pythonPatch, (New-Object System.Text.UTF8Encoding($false)))\n    Invoke-NativeCommand -FilePath 'python' -Arguments @($patchScript, $worktree) -Description 'Patch script' | Out-Null\n    $validationResults.Add('patch_script: pass') | Out-Null\n    Invoke-NativeCommand -FilePath 'python' -Arguments @('-m','py_compile', (Join-Path $worktree 'project_sources/collector/tools/build_powershell_surface_inventory.py'), (Join-Path $worktree 'project_sources/collector/tools/run_powershell_assembly_parity.py'), (Join-Path $worktree 'project_sources/collector/tools/run_powershell_custom_checks.py'), (Join-Path $worktree 'project_sources/collector/tools/test_build_powershell_surface_inventory_path_safety.py'), (Join-Path $worktree 'project_sources/collector/tools/test_run_powershell_assembly_parity.py'), (Join-Path $worktree 'project_sources/collector/tools/test_run_powershell_custom_checks.py')) -Description 'py_compile focused files' | Out-Null\n    $validationResults.Add('py_compile_focused: pass') | Out-Null\n    Invoke-NativeCommand -FilePath 'python' -Arguments @((Join-Path $worktree 'project_sources/collector/tools/test_build_powershell_surface_inventory_path_safety.py')) -Description 'inventory path-safety tests' | Out-Null\n    $validationResults.Add('inventory_path_safety_tests: pass') | Out-Null\n    Invoke-NativeCommand -FilePath 'python' -Arguments @((Join-Path $worktree 'project_sources/collector/tools/test_run_powershell_assembly_parity.py')) -Description 'assembly parity tests' | Out-Null\n    $validationResults.Add('assembly_parity_tests: pass') | Out-Null\n    Invoke-NativeCommand -FilePath 'python' -Arguments @((Join-Path $worktree 'project_sources/collector/tools/test_run_powershell_custom_checks.py')) -Description 'custom check tests' | Out-Null\n    $validationResults.Add('custom_check_tests: pass') | Out-Null\n    Invoke-NativeCommand -FilePath 'python' -Arguments @('-m','unittest','discover','-s',(Join-Path $worktree 'project_sources/collector/tools'),'-p','test_*.py') -Description 'collector tools unittest discovery' | Out-Null\n    $validationResults.Add('collector_tools_unittest_discovery: pass') | Out-Null\n    Invoke-NativeCommand -FilePath 'python' -Arguments @((Join-Path $worktree 'scripts/validate-codeql-security-workflow.py')) -Description 'CodeQL workflow validator' | Out-Null\n    $validationResults.Add('codeql_workflow_validator: pass') | Out-Null\n    Invoke-Git -Cwd $worktree -GitArgs @('diff','--check','--') | Out-Null\n    $validationResults.Add('git_diff_check: pass') | Out-Null\n    Invoke-Git -Cwd $worktree -GitArgs @('config','user.name','github-actions[bot]') | Out-Null\n    Invoke-Git -Cwd $worktree -GitArgs @('config','user.email','41898282+github-actions[bot]@users.noreply.github.com') | Out-Null\n    Invoke-Git -Cwd $worktree -GitArgs (@('add','--') + $expectedPaths) | Out-Null\n    Invoke-Git -Cwd $worktree -GitArgs @('diff','--cached','--check') | Out-Null\n    $validationResults.Add('git_diff_cached_check: pass') | Out-Null\n    $changedNames = Get-GitText -Cwd $worktree -GitArgs @('diff','--cached','--name-only')\n    $actualPaths = @($changedNames -split \"`r?`n\" | Where-Object { -not [string]::IsNullOrWhiteSpace($_) })\n    $unexpected = @($actualPaths | Where-Object { $expectedPaths -notcontains $_ })\n    $missing = @($expectedPaths | Where-Object { $actualPaths -notcontains $_ })\n    if ($unexpected.Count -ne 0 -or $missing.Count -ne 0) { throw \"Cached diff paths mismatch. Actual paths:`n$changedNames\" }\n    Invoke-Git -Cwd $repo -GitArgs @('fetch','origin',$fetchRefspec) | Out-Null\n    $prePushHead = Get-GitText -Cwd $repo -GitArgs @('rev-parse',\"origin/$branch\")\n    if ($prePushHead -ne $expectedHead) { throw \"PR branch moved before push. Expected $expectedHead but origin/$branch is $prePushHead.\" }\n    $commitParent = Get-GitText -Cwd $worktree -GitArgs @('rev-parse','HEAD')\n    Invoke-Git -Cwd $worktree -GitArgs @('commit','-m','Reject symlink escapes across PowerShell path readers','-m','Fail closed before hashing or reading symlinked inventory, harness, and custom-check targets that resolve outside the repository.') | Out-Null\n    $newHead = Get-GitText -Cwd $worktree -GitArgs @('rev-parse','HEAD')\n    $token = Get-RequiredGitToken\n    Invoke-GitPushWithToken -Cwd $worktree -Token $token -Refspec $pushRefspec | Out-Null\n    Invoke-Git -Cwd $repo -GitArgs @('fetch','origin',$fetchRefspec) | Out-Null\n    $remoteReadbackHead = Get-GitText -Cwd $repo -GitArgs @('rev-parse',\"origin/$branch\")\n    if ($remoteReadbackHead -ne $newHead) { throw \"Push readback mismatch. New commit $newHead but origin/$branch reads $remoteReadbackHead.\" }\n    Write-Host \"DCOIR_EXEC_RESULT: success\"\n    Write-Host \"DCOIR_EXEC_REQUEST_ID: $requestId\"\n    Write-Host \"DCOIR_EXEC_BRANCH: $branch\"\n    Write-Host \"DCOIR_EXEC_PARENT: $commitParent\"\n    Write-Host \"DCOIR_EXEC_NEW_HEAD: $newHead\"\n    Write-Host \"DCOIR_EXEC_CHANGED_PATHS: $($actualPaths -join ',')\"\n    Write-Host \"DCOIR_EXEC_VALIDATION: $($validationResults -join '; ')\"\n}\nfinally {\n    try {\n        if (Test-Path -LiteralPath $worktree) { Invoke-Git -Cwd $repo -GitArgs @('worktree','remove','--force',$worktree) | Out-Null }\n    } catch { Write-Warning \"Temporary worktree cleanup failed: $($_.Exception.Message)\" }\n}"
}
