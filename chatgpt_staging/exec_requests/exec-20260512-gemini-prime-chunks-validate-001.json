{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "exec-20260512-gemini-prime-chunks-validate-001",
  "operator_approved": true,
  "approved_command_preview": "Run Gemini release build validation after prime-agent chunk refactor and write reports to artifact readback only.",
  "shell": "pwsh",
  "timeout_seconds": 900,
  "artifact_retention_days": 7,
  "cleanup_request_after_run": true,
  "command": "$ErrorActionPreference = 'Stop'\n$repo = $env:DCOIR_REPO_ROOT\n$out = Join-Path $env:DCOIR_DOWNLOADS_DIR 'gemini_prime_chunks_validate'\nNew-Item -ItemType Directory -Force -Path $out | Out-Null\npython (Join-Path $repo 'project_sources/gemini/tools/build_dcoir_gemini_release.py') --source-root (Join-Path $repo 'project_sources/gemini/bundle_source') --output-dir $out\n"
}
