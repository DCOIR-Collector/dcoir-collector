{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "exec-20260506-dlt-airtable-to-supabase-002",
  "operator_approved": true,
  "approved_at_utc": "2026-05-06T00:00:00Z",
  "approved_command_preview": "Retry dlt one-for-one Airtable to Supabase import after exposing DCOIR_SUPABASE_POSTGRES_URL to chatgpt-exec. Target Supabase schema/dataset airtable_import and report table counts.",
  "command": "$ErrorActionPreference = 'Stop'\n$repo = [Environment]::GetEnvironmentVariable('DCOIR_REPO_ROOT','Machine')\nif ([string]::IsNullOrWhiteSpace($repo)) { throw 'Missing DCOIR_REPO_ROOT' }\n$wrapper = Join-Path $repo 'chatgpt_staging\\scripts\\run_dlt_airtable_to_supabase.ps1'\nif (-not (Test-Path -LiteralPath $wrapper -PathType Leaf)) { throw \"Missing wrapper: $wrapper\" }\n& $wrapper",
  "shell": "powershell_5",
  "timeout_seconds": 1200,
  "artifact_retention_days": 30,
  "cleanup_request_after_run": true
}