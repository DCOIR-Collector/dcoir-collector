diff --git a/.github/workflows/reusable-ops-apply-patch.yml b/.github/workflows/reusable-ops-apply-patch.yml
--- a/.github/workflows/reusable-ops-apply-patch.yml
+++ b/.github/workflows/reusable-ops-apply-patch.yml
@@ -240,7 +240,8 @@
           except (OSError, json.JSONDecodeError):
               print("false")
               raise SystemExit
 
-          print("true" if result.get("result") == "success" and result.get("mode") == "apply" else "false")
+          operation = result.get("operation") or result.get("mode")
+          print("true" if result.get("result") == "success" and operation == "apply" else "false")
           PY
           )"
