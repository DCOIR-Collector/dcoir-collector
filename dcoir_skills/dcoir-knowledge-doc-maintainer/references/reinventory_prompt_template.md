Reinventory the current GitHub-primary governed readable source set and retained supporting outputs after a Knowledge-doc refresh under the current GitHub-primary knowledge-doc model. I refreshed the human-readable Knowledge docs in `knowledge/*.md` and refreshed the retained `supporting_assets/supporting_knowledge_docs.zip` delivery asset as needed. Determine whether any associated project files, settings references, helper skills, README surfaces, routing-note surfaces, or bundle mappings now need refresh so the workspace remains aligned under the current control plane and naming model, and if so generate the exact updated file set and release instructions needed for the appropriate grouped update or bundle.
