Reinventory the current GitHub-primary DCOIR source set and any retained supporting outputs after this Knowledge-doc refresh. I refreshed the governed human-readable Knowledge docs and any retained delivery ZIP that still needs to stay aligned. Determine whether any associated project files, project settings references, helper skills, or bundle mappings need refresh so the workspace remains aligned under the current control plane and naming model, and if so generate the exact updated file set and release instructions needed for the appropriate grouped update.
