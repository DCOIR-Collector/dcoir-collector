DCOIR Scheduled Health-Check Residue-Order Fix - 2026-04-05

Current state id: 2026-04-05-scheduled-health-check-residue-order-fix-v1

Purpose
- Record the focused correction needed after live manual execution of scheduled-health-check exposed one remaining workflow self-conflict.

What happened
- The workflow passed its required-surface checks and then reached the packaging-residue step.
- The failure showed a generated `__pycache__/render_attention_signal.cpython-311.pyc` path under the checked-out repo tree.
- The same workflow performs Python syntax validation earlier by running `python -m py_compile` over tracked Python files.
- That means the workflow was failing on residue created by its own earlier validation step, not necessarily on pre-existing committed residue.

Fix decision
- Keep the packaging-residue check, but move it earlier in the workflow so it runs before Python syntax validation.
- Do not widen the change set beyond this one focused reordering correction.

Why it matters
- The first-wave automation baseline was almost fully stabilized, but this one workflow path was still unproven.
- Correcting the step order preserves the package-cleanliness policy without letting the workflow trip over its own generated files.
- After one successful rerun, the project can honestly return to the stable baseline posture.

Best next move
- Apply this focused scheduled-health-check fix.
- Re-run scheduled-health-check once.
- If it passes, return to deliverables-first with the later approved platform-adoption queue unchanged.
