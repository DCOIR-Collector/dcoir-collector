DCOIR Collector Operator GitHub Primary And Control Plane Pair Fix - 2026-04-05

Current state id: 2026-04-05-collector-operator-github-primary-and-control-plane-pair-fix-v1

Purpose
- Record the bounded collector/operator follow-through alignment pass across `dcoir-operator-workflow-hardener` and `dcoir-collector-qa`, together with correction of the live control-plane version-pair mismatch left by the previous grouped update.

Decision summary
- Repair `dcoir-operator-workflow-hardener` so it uses GitHub-primary governed-readable-source language consistently instead of older uploaded-source wording when reasoning from the current collector source and choosing operator execution lanes.
- Repair `dcoir-collector-qa` so the current GitHub-readable collector and harness sources are the default QA scope, rollback material is limited to explicit comparison or historical-regression use, and the maintenance contract no longer treats a local harness CMD wrapper as a maintained default command block.
- Keep rollback material available as bounded historical comparison input instead of silently dropping it, but stop treating it as a primary default source line.
- Correct the live control-plane version pair by bringing `CP-02` forward to match the already-live `CP-01` branch and then advancing both control-plane surfaces together in one bounded grouped update.

Why it matters
- The coordinated campaign found that the collector-follow-through family still carried wording from older uploaded-source or mirror-oriented assumptions even though the current DCOIR working line is GitHub-primary.
- The same review found a live control-plane defect: `CP-01` had already advanced to 3.9.47 while `CP-02` still reported 3.9.46, which would eventually undermine the drift gate if left uncorrected.
- Grouping the collector/operator wording fixes with the control-plane pair correction reduces operator friction and avoids surfacing a separate one-off patch just to repair the version mismatch.

Validation notes
- The updated `dcoir-operator-workflow-hardener` and `dcoir-collector-qa` skill packages pass clean packaging validation with no runtime-residue entries included.
- Reference review confirms that operator guidance and collector QA now point at the current GitHub-readable collector and harness sources, that rollback material is treated as bounded historical comparison input rather than a default authoritative source, and that the outdated local harness CMD wrapper requirement is removed from the maintained default command-block set.
- The refreshed control-plane and continuity surfaces now share one aligned version pair and one shared `current_state_id` for this branch.

Next immediate move
- Continue the coordinated campaign into the next workflow-state, authority, or collector-follow-through helper-skill pass while holding compatible fixes for the next meaningful grouped manual update/install wave.
