DCOIR Gemini Agent Build Live Test Findings - 2026-04-07

Current state id
- 2026-04-07-gemini-upload-budget-and-validation-automation-queue-v1

Status
- These are operator live-test findings captured during a work-safe browser-first session.
- They are review notes for later build and output-route improvement.
- They are not yet treated as fixed unless the control plane later promotes them into governed current-state surfaces.

Operator preferences and output-route requirements
- Main agent should ship with three starter prompts in the next build.
- Prime-agent and sub-agent markdowns should merge Agent role or purpose plus Description into one best-of-breed field named Description so the markdowns map cleanly into Agent Designer.
- Instructions should be standardized to the "You are ..." style across the main agent and sub-agents.

Live-test findings to verify and likely remediate
- The primary agent appears to over-focus on Elastic Defend triage and does not visibly state that it also triages or interprets DCOIR collector artifacts.
- IOC-to-OSINT lookup ownership is not visibly explicit in the current agent set.
- If either capability already exists, the current issue may still be a visibility or discoverability problem and should be corrected in the next build.

Validation additions requested for the next bundle
- Add a collector-artifact test plan so the collector-artifact interpretation path is explicitly tested.
- Add a collector execution or collection-path test plan that covers collection success, harness-style paths such as FullRegression or core-style runs, and any pull or retrieval behavior needed to generate agent-consumable artifacts.

Research item
- Research whether Google Gemini Enterprise Agents can support an attached collector zip plus PowerShell script model and expose usable links to those assets for users on request.
- Treat this as a capability-check topic, not an approved delivery pattern yet.

Post-session measured office Gemini limits
- Practical maximum single-file attachment size in the office Gemini Enterprise Agent environment: 1000 KB.
- Practical maximum total attachment size per prompt in the same environment: 2000 KB.
- Safe working targets derived from those limits: 900 KB or less per file and 1800 KB or less total attachments per prompt.
- Session-total file capacity remains unknown and should stay an open live-test item.

Follow-on queue extended after the measured limit test
- Finalize an upload-budget-aware artifact-intake pattern for baseline, enrichment, retrieved-artifact, and final-synthesis review.
- Revisit the purpose of the baseline report surface because a broad default baseline file may no longer be the best Gemini-facing artifact under the measured limit.
- Build a deterministic generation-verification action or workflow for the Gemini bundle and the standalone all-in-one prompt bundle.
- Build explicit generated-bundle quality-control review criteria.
- Plan future automation for testing the standalone all-in-one prompt bundle and the Gemini parent plus sub-agent bundle with fake Elastic alerts and fake collector artifacts.
- Verify installed-skill parity against governed GitHub source.
- Evaluate whether an optional Google Drive staging folder would reduce non-governed document-handling friction without violating the GitHub-primary readable-source rule.

Suggested next review focus
1. Upload-budget-aware artifact intake and baseline-surface redesign.
2. Deterministic generation verification for both bundle lines.
3. Generated-bundle quality-control criteria.
4. Collector-artifact and collector-execution test-plan coverage.
5. Future automation planning for standalone and Gemini bundle validation.

Truth boundary
- This note preserves live-test observations and requested follow-up.
- It does not by itself change the current governed control plane or prove the findings are already fixed.
