DCOIR Plan-Tracker Verbose Resume-State And Widening-Hold Workflow Decision - 2026-04-04

Purpose
- Record the approved 2026-04-04 workflow decisions for treating `dcoir-plan-tracker` as the next stateful-skill reference candidate after the tracker, enforcement, and direct-resume implementations.
- Turn the stamped-surface widening hold into an explicit counted governance item instead of leaving it as a chat-only suggestion.

Approved durable workflow decisions
- `dcoir-plan-tracker` is the next bounded stateful-skill candidate for explicit verbose resume-state and flush/manicure treatment.
- Operator-facing plan resume state should be verbose enough that a future session can understand the carried state without reconstructing prior chat context.
- The minimum verbose plan-resume standard is: active task, why the current task matters, resume detail, carry-forward note, next recommended action, flush trigger, pending flush items, promotion candidates, remain-local notes, and blockers.
- A valid plan-level flush/manicure check must inspect the current plan state and active task, surface buffered state, surface what is safe to flush now, surface what should remain local for now, surface the next flush trigger, and end with one best next move.
- Deferred governance decisions that depend on repeated validation may be tracked through an explicit countdown rather than vague prose reminders when that makes follow-through more durable.
- The stamped active-surface widening review remains deferred and should now be tracked as a countdown item with 2 remaining real resume cycles before any widening review is reconsidered.
- Decrement the widening-hold countdown only after a qualifying real resume cycle that uses the direct drift gate on live governed state and is judged acceptable.
- When the widening-hold countdown reaches 0, trigger a bounded widening review. Do not widen the stamped active-surface set automatically without that review.

Reference plan-tracker flush/manicure sequence
1. Re-anchor to Project Instructions, then `CP-01`, then `CP-02`.
2. Inspect the current plan state, active task, and `05_resume_state.md`.
3. Surface buffered or unsettled plan-state changes, blocker-recovery notes, promotion candidates, remain-local notes, and pending flush items.
4. Surface what is safe to flush now.
5. Surface what should remain local or plan-buffered for now.
6. Surface the next flush trigger and one best next move.
7. When the current branch includes a countdown-gated review, surface the remaining count and the trigger condition explicitly.

Implementation posture
- Patch the plan-tracker reference path first, then validate it before widening the same pattern to additional stateful skills.
- Keep the widening hold visible in governed todo or continuity surfaces while it remains active.
- Treat the countdown as a coordination aid, not as an automatic policy engine.
- Continue to treat plan state as continuity-rich helper state, not as control-plane authority.

Immediate validation targets
1. Confirm that a generated plan can render richer operator-facing resume state.
2. Confirm that buffered plan-state items, remain-local notes, promotion candidates, and flush triggers are explicit in `05_resume_state.md`.
3. Confirm that the widening-hold countdown is visible and machine-readable enough in plan state to avoid being forgotten.
4. Confirm that the patched plan-tracker skill package validates cleanly.
5. Decide which remaining stateful skill should be reviewed next after the patched plan-tracker path is validated.

Best next move
- Run bounded regression on the patched `dcoir-plan-tracker` path, keep the widening-hold countdown explicit at 2 remaining real resume cycles, and only then decide whether to move the same pattern into the next stateful skill.
