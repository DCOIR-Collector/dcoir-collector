DCOIR Active Now

Current state id: 2026-04-04-resume-gate-v1

1. Run bounded regression on the direct `dcoir-session-resume` consumption of the `dcoir-source-authority-auditor` drift gate.
   Why it matters:
   - The project now has an explicit drift gate and a machine-checkable active-surface coordination signal, but the resume entry point still needs proof that it stops correctly on drift and resumes cleanly when the active continuity surfaces are aligned.
   Next action:
   - Use aligned and intentionally drifted active-surface fixtures as the acceptance tests, then inspect whether the stop report and the clean resume path are explicit enough to support quick operator understanding and grouped remediation.

2. Decide whether the stamped active-surface set should widen beyond the initial continuity set.
   Why it matters:
   - The initial stamped surface set is now aligned again and the first explicit drift gate is live, but the project still needs to decide whether a larger stamped set would improve correctness enough to justify the extra maintenance.
   Next action:
   - After the bounded direct-resume validation pass, inventory the adjacent continuity surfaces and decide whether widening the stamped set improves real workflow safety without adding avoidable noise.

3. Decide which other `dcoir-*` skills truly need the same stateful flush/manicure and verbose-state treatment.
   Why it matters:
   - `dcoir-session-tracker` is the first proving ground, but other stateful helper skills may also need explicit pre-write state surfacing and richer operator-facing continuity.
   Next action:
   - After the tracker, enforcement, and direct-resume reference implementations are validated, inventory the other stateful skills and decide which ones should adopt the same pattern.

4. Use canonical task-memory preflight before GitHub-family execution work.
   Why it matters:
   - Validated procedures should be consulted before friction, not after avoidable misses or retries.
   Next action:
   - Check knowledge/task_memory and the applicable GitHub procedures before repo writes, deletes, packaging, or structural changes.

5. Treat post-create and post-update helper-skill regression as mandatory.
   Why it matters:
   - Packaging success is not sufficient proof that a helper skill is ready for broader workflow use.
   Next action:
   - Pass every helper-skill create or update through dcoir-skill-regression-auditor and keep bounded evidence when full integrated regression is not yet practical.
