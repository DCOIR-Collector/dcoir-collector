DCOIR Active Now

Current state id: 2026-04-04-session-tracker-presence-guard-and-delivery-preference-v1

1. Live-validate the updated `dcoir-session-tracker` presence preflight, explicit absence or re-init signaling, and pre-push review or todo-sync behavior during the next suitable governed push.
   Why it matters:
   - The tracker now detects and surfaces missing-local-file recovery more honestly, but that path still needs a real grouped-push validation for operator trust.
   Next action:
   - Use the updated tracker on the next governed push and verify that the presence preflight and explicit re-init signaling are strong enough in practice.

2. Continue the broader helper-skill deep-dive and code-hygiene campaign from the stronger tracker baseline.
   Why it matters:
   - The project now has a validation regime, parity baseline, discovery contract, and a materially stronger tracker path, so deeper execution and code-hygiene review can proceed with less hidden-state risk.
   Next action:
   - After the live tracker validation, rank the next deep-dive targets and begin the next highest-value execution and code review pass.

3. Maintain the stamped active-surface widening hold countdown at 2 remaining real resume cycles before any widening review.
   Why it matters:
   - The project wants evidence before widening the stamped active-surface set, and synthetic or generated-artifact validation should not count as real live resume cycles.
   Next action:
   - Decrement the countdown only after a qualifying real resume cycle that uses the direct drift gate on live governed state and is judged acceptable.

4. Review any remaining hard-coded literals only as bounded contractual exceptions.
   Why it matters:
   - The practical high-value discovery-contract adoption pass is materially complete, so any remaining literals should now be treated as explicit contract review rather than as default embedded assumptions.
   Next action:
   - Review remaining literals only where portability, maintainability, or governance clarity would materially improve.
