DCOIR Active Now

1. Run bounded regression on the verbose `dcoir-session-tracker` upgrade and the pre-GitHub-update flush/manicure workflow.
   Why it matters:
   - The tracker is now the reference model for stateful helper-skill session state, but it still needs explicit proof that the richer capture, export, and flush/manicure behavior improves fresh-session resume and operator understanding in practice.
   Next action:
   - Use the startup-speed or fresh-session resume concern and the governance-gap concern as the acceptance-test cases, then inspect whether the resulting resume path is quick, predictable, and repeatable enough.

2. Establish an authoritative current-state and version-checking mechanism across control-plane, continuity, and todo surfaces.
   Why it matters:
   - The recent lag between LOG-01, CP-01, CP-02, LOG-03, and LOG-05 showed that the project can drift into contradictory state unless there is a durable verification and enforcement model.
   Next action:
   - Define the source of truth, the required verification cadence, the drift-detection method, the attention-worthy callout path, and the remediation workflow that keeps active surfaces on the same page.

3. Decide which other `dcoir-*` skills truly need the same stateful flush/manicure and verbose-state treatment.
   Why it matters:
   - `dcoir-session-tracker` is the first proving ground, but other stateful helper skills may also need explicit pre-write state surfacing and richer operator-facing continuity.
   Next action:
   - After the tracker regression passes, inventory the other stateful skills and decide which ones should adopt the same pattern.

4. Use canonical task-memory preflight before GitHub-family execution work.
   Why it matters:
   - Validated procedures should be consulted before friction, not after avoidable misses or retries.
   Next action:
   - Check knowledge/task_memory and the applicable GitHub procedures before repo writes, deletes, packaging, or structural changes.

5. Treat post-create and post-update helper-skill regression as mandatory.
   Why it matters:
   - Packaging success is not sufficient proof that a helper skill is ready for broader workflow use.
   Next action:
   - Pass every helper-skill create or update through dcoir-skill-regression-auditor and keep bounded evidence when full integrated regression is not yet practical.
