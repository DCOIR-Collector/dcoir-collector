DCOIR Active Now

Current state id: 2026-04-05-scheduled-health-check-residue-pattern-fix-v1

1. Apply the focused scheduled-health-check residue-pattern fix now.
   Why it matters:
   - The workflow is currently failing on an invalid regex pattern rather than on real residue or on the DCOIR harness.
   Next action:
   - Replace the regex-based residue matcher with safe path and extension checks.

2. Rerun scheduled-health-check once after the fix lands.
   Why it matters:
   - That one rerun is needed to prove the full first-wave baseline end to end.
   Next action:
   - Trigger scheduled-health-check manually on main and confirm it passes.

3. If it passes, return to the stable-baseline posture.
   Why it matters:
   - The project can then stop churning on the automation baseline and keep the main line on deliverables.
   Next action:
   - Leave the baseline alone unless a real defect appears, and preserve the later approved platform-adoption queue for bounded future work.
