DCOIR Active Now

Current state id: 2026-04-04-hardcoded-audit-v1

1. Continue the hard-coded-project-variable audit across remaining skills and workflow surfaces.
   Why it matters:
   - The operator wants DCOIR helper skills to evolve with the project rather than repeatedly breaking on renamed files, moved control-plane surfaces, or transferred workflow contexts.
   Next action:
   - Use the new governed discovery contract where it replaces brittle bootstrap literals and identify the next highest-value skills or workflow surfaces still relying on repeated project-specific assumptions.

2. Keep the CI/CD-style validation regime active and use the parity/discovery outputs to anchor the broader helper-skill deep-dive and code-hygiene campaign.
   Why it matters:
   - The project now has a grounded inventory, parity surface, and first-pass discovery contract, so the deeper audit can be scoped from real current evidence rather than from chat memory.
   Next action:
   - Rank the next deep-dive targets and begin the highest-value execution and code review passes.

3. Use the shared pre-push flush and staged-governed-update contract for session-memory-enabled or buffer-capable skills.
   Why it matters:
   - Tracker and helper-memory state can still be missed when a suitable governed push is already happening in the same branch.
   Next action:
   - Before future governed pushes, require a bounded flush/manicure review for the buffer-capable skills and stage same-branch governed updates plus post-push cleanup.

4. Maintain the stamped active-surface widening hold countdown at 2 remaining real resume cycles before any widening review.
   Why it matters:
   - The project wants evidence before widening the stamped active-surface set, and synthetic or generated-artifact validation should not count as real live resume cycles.
   Next action:
   - Decrement the countdown only after a qualifying real resume cycle that uses the direct drift gate on live governed state and is judged acceptable.
