DCOIR Active Now

Current state id: 2026-04-04-plan-tracker-v2

1. Maintain the stamped active-surface widening hold countdown at 2 remaining real resume cycles before any widening review.
   Why it matters:
   - The project wants evidence before widening the stamped active-surface set, and that decision should remain explicit, countable, and review-triggering rather than automatic.
   Next action:
   - Do not decrement the hold for generated-plan validation artifacts; after each qualifying real resume cycle that uses the direct drift gate on live governed state and is judged acceptable, decrement the hold countdown in the governed todo or continuity surfaces and trigger a bounded widening review only when the countdown reaches 0.

2. Decide which additional `dcoir-*` skills truly need the same stateful flush/manicure and verbose-state treatment next.
   Why it matters:
   - `dcoir-session-tracker`, `dcoir-source-authority-auditor`, `dcoir-session-resume`, and the repaired `dcoir-plan-tracker` path are the first proving grounds, but other stateful helper skills may also need explicit pre-write state surfacing and richer operator-facing continuity.
   Next action:
   - Review the next strongest remaining stateful skill candidate and decide whether it should adopt the same pattern next.

3. Use canonical task-memory preflight before GitHub-family execution work.
   Why it matters:
   - Validated procedures should be consulted before friction, not after avoidable misses or retries.
   Next action:
   - Check knowledge/task_memory and the applicable GitHub procedures before repo writes, deletes, packaging, or structural changes.

4. Treat post-create and post-update helper-skill regression as mandatory.
   Why it matters:
   - Packaging success is not sufficient proof that a helper skill is ready for broader workflow use.
   Next action:
   - Pass every helper-skill create or update through dcoir-skill-regression-auditor and keep bounded evidence when full integrated regression is not yet practical.
