DCOIR Active Now

1. Govern the GitHub connector reference pack and promote its strongest execution patterns into durable GitHub procedure guidance and task memory.
   Why it matters:
   - GitHub connector execution now materially shapes repo workflow speed, reliability, and recoverability.
   Next action:
   - Use knowledge/github_connector_reference/, GH-PROC-008, and canonical GitHub task memory to refine function selection, operator guidance, and future GitHub-heavy workflow planning.

2. Finish the governed structural refactor standard for growth-prone readable files.
   Why it matters:
   - The project has a validated collector split pattern but does not yet have a fully codified repo-wide structural standard.
   Next action:
   - Align CP-01, CP-02, DOC-01, DOC-03, LOG-01, LOG-03, and the todo structure to DOC-04 and keep the structural pass current while connector-governance improvements land.

3. Inventory remaining likely split candidates, legacy split-attempt artifacts, naming-normalization targets, and lagging repo delivery artifacts under the new standard.
   Why it matters:
   - The new standard should immediately guide the next real architecture targets rather than remain abstract.
   Next action:
   - Review growth-prone governed files, orphaned split-attempt artifacts, and repo delivery artifacts that may still describe the older posture.

4. Resume the extracted readable Gemini and supporting-knowledge folder integration pass under the new structural rules.
   Why it matters:
   - The extracted readable working line still needs better governed navigation and long-term placement, but it should proceed under the new structural standard.
   Next action:
   - Audit the extracted folders after the structural pass and promote the highest-value materials into stronger indexes, docs, and governed deliverable references.

5. Use canonical task-memory preflight before GitHub-family execution work.
   Why it matters:
   - Validated procedures should be consulted before friction, not after avoidable misses or retries.
   Next action:
   - Check knowledge/task_memory and the applicable GitHub procedures before repo writes, deletes, packaging, or structural changes.

6. Prefer one bounded multi-file git-object transaction when multiple related existing-file edits or deletions belong together.
   Why it matters:
   - Grouped repo changes reduce drift and mirror programmer-style all-at-once updates better than one-file-at-a-time churn.
   Next action:
   - Batch related GitHub changes into one transaction and verify every touched path afterward.

7. Upgrade the core helper-skill workflow so memory-preflight is closed-loop instead of pre-execution only.
   Why it matters:
   - Reusable lessons from blockers and failed attempts should improve future workflow speed and quality rather than remain trapped in one chat or one plan.
   Next action:
   - Update dcoir-memory-preflight, dcoir-decision-policy, dcoir-plan-tracker, and dcoir-session-tracker so blocker recovery can be classified and staged for later promotion.

8. Implement session-local write buffers and grouped GitHub flush behavior for the highest-value stateful helper skills.
   Why it matters:
   - Stateful helper skills should not need to push every tiny change immediately when a bounded grouped flush would be cleaner and more repeatable.
   Next action:
   - Start with dcoir-plan-tracker, dcoir-session-tracker, dcoir-decision-policy, dcoir-memory-preflight, and dcoir-skill-regression-auditor, using explicit flush-check triggers rather than pretending chat can silently monitor elapsed time.

9. Treat post-create and post-update helper-skill regression as mandatory.
   Why it matters:
   - Packaging success is not sufficient proof that a helper skill is ready for broader workflow use.
   Next action:
   - Pass every helper-skill create or update through dcoir-skill-regression-auditor and keep bounded evidence when full integrated regression is not yet practical.

10. Finish the governed rollout model for GitHub-hosted DCOIR helper-skill source.
    Why it matters:
    - The project needs a repeatable path for getting new and updated helper-skill source under GitHub governance without dual-maintenance drift.
    Next action:
    - Use the approved workflow of build, download, manual install, extract, and GitHub Desktop source ingest for new skills, and build, download, manual install, and assistant-managed GitHub source update for already-governed skills.
