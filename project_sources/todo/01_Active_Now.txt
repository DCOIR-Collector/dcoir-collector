DCOIR Active Now

1. Analyze the best governed implementation path for a GitHub-native skills inventory note that lists current dcoir-* skills, intended use cases, and trigger phrases without changing authority or implying deterministic auto-invocation.
   Why it matters:
   - The project needs a durable, governed way to keep helper-skill awareness visible without turning a documentation note into an authority override or a hard routing mechanism.
   Next action:
   - Determine the best file class, repo location, scope, and wording for the note.
   - Verify that the note can remain descriptive only and does not conflict with existing authority, workflow, or helper-skill boundary rules.
   - Identify any direct refresh set that would be required only if the note is later implemented.

2. If the skills-inventory analysis supports it, implement the note as descriptive documentation only and refresh any directly affected control-plane or workflow docs.
   Why it matters:
   - The note should only exist if it improves governed skill awareness without creating authority drift or false expectations about skill invocation.
   Next action:
   - Implement the note only after the analysis settles its location, scope, and wording, then refresh any directly affected awareness or control-plane docs.

3. Resume main project deliverables and reprioritize remaining todo items from the split todo structure after the skills-inventory decision is settled.
   Why it matters:
   - The project still needs to return to the real deliverables and broader todo lanes once this documentation-design decision is clear.
   Next action:
   - Choose the top deliverable or remaining todo lane and continue from the current split todo structure after the skills-inventory decision is made.

4. Use canonical task-memory preflight before GitHub-family execution work.
   Why it matters:
   - Validated procedures should be consulted before friction, not after an avoidable miss or retry.
   Next action:
   - Check knowledge/task_memory and the applicable GitHub procedures before repo writes, deletes, packaging, or structural changes.

5. Prefer one bounded multi-file git-object transaction when multiple related existing-file edits or deletions belong together.
   Why it matters:
   - Grouped repo changes reduce drift and mirror programmer-style all-at-once updates better than one-file-at-a-time churn.
   Next action:
   - Batch related GitHub changes into one transaction and verify every touched path afterward.
