DCOIR Active Now

Current state id: 2026-04-06-deliverables-first-structure-recentering-v1

1. Keep the real project deliverables explicit.
   Why it matters:
   - The prompt-pack line, standalone analyst-facing master prompt, PP-09 and PP-10 Gemini design line, and the generated/comparative markdown surfaces are the actual project end-state, not side notes behind the automation framework.
   Next action:
   - Use the current continuity surfaces and repo guide files to keep those deliverables named explicitly in future work instead of referring to deliverables only in the abstract.

2. Maintain the authoritative prompt-pack and standalone prompt line.
   Why it matters:
   - PP-01 through PP-07 are the behavioral authority and PP-08 is the current standalone runtime prompt that analysts can use directly.
   Next action:
   - Edit PP-01 through PP-07 when behavior changes, then regenerate and verify PP-08 rather than patching PP-08 directly.

3. Maintain the current Gemini Enterprise design line.
   Why it matters:
   - PP-09 and PP-10 are the current design workflow and bounded design artifact for the merged parent-plus-seven-sub-agent topology that covers Elastic alert triage, DCOIR collection, IOC enrichment, artifact review, and final synthesis.
   Next action:
   - Keep PP-09, PP-10, and the generated/comparative markdown surfaces aligned, and do not let older extracted bundle material pose as the current operator build authority.

4. Keep the old Gemini email build bundle demoted to legacy extracted reference.
   Why it matters:
   - The old quick-start path still reflects an older one-prime-plus-four-sub-agent shape and can mislead the operator if it is not clearly marked.
   Next action:
   - Treat that folder as readable legacy reference only until a later bounded refresh rebuilds it to the current topology.

5. Keep the deliverable packaging line complete.
   Why it matters:
   - The generated and comparative Gemini markdowns are easier to move, inspect, and reuse when their retained supporting ZIPs are kept alongside supporting_knowledge_docs.zip.
   Next action:
   - Refresh the generated/comparative/knowledge ZIP assets whenever their readable source sets change materially.
