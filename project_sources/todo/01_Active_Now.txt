DCOIR Active Now

Current state id: 2026-04-06-work-safe-browser-first-and-manual-write-posture-alignment-v1

1. Keep the fully re-stabilized first-wave GitHub automation baseline in place while the main project line stays on deliverables.
   Why it matters:
   - The automation baseline remains proven end to end after the cleanup-era corrections and the passing scheduled-health-check rerun.
   Next action:
   - Leave the baseline alone unless a real defect appears, and keep the main project effort on deliverables.

2. Keep the work-safe browser-first operating constraint explicit.
   Why it matters:
   - The full transcript pass confirmed that the operator's real work environment should not depend on uploads or downloads during work hours when an equivalent browser-native lane exists.
   Next action:
   - Prefer browser-native, no-upload or download required workflows during work hours whenever they can achieve the same governed result.

3. Keep GitHub Desktop manual grouped repo-update delivery as the default governed write posture.
   Why it matters:
   - The transcript confirmed that the normal governed write lane should stay manual and grouped rather than drifting toward a PR-first or in-chat direct-write default.
   Next action:
   - Use GitHub Desktop manual grouped repo-update delivery by default, and keep PR or review flows optional and bounded.

4. When ready, start with a bounded Copilot code review pilot.
   Why it matters:
   - It is the smallest approved next pilot and keeps pull requests optional rather than forcing a new daily workflow.
   Next action:
   - Use Copilot code review only on review-worthy or higher-risk PRs when the operator explicitly opens that pilot.

5. After that, keep the remaining approved pilot order intact.
   Why it matters:
   - The operator approved the broader set and wants the sequence remembered without having to reconstruct it later.
   Next action:
   - Follow this order when later work opens: Copilot cloud agent, GitHub Models, selective OpenAI Codex, then later conditional GitHub Pages, code scanning if eligible and worthwhile, self-hosted runners if needed, OIDC if needed, and artifact provenance or attestation or SLSA-style work later.
