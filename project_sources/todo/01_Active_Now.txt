DCOIR Active Now

Current state id: 2026-04-04-stateful-trio-v1

1. Run bounded regression on the coordinated stateful-candidate trio campaign.
   Why it matters:
   - `dcoir-decision-policy`, `dcoir-memory-preflight`, and `dcoir-skill-regression-auditor` are the strongest remaining stateful or workflow-control candidates after the earlier reference implementations, and the operator explicitly wanted a coordinated batch instead of a slow trickle of one-off pushes.
   Next action:
   - Use representative operator-preference capture, post-blocker promotion-candidate staging, and coordinated multi-skill regression-plan output as the acceptance tests, then confirm that the trio is explicit enough for real workflow use.

2. Maintain the stamped active-surface widening hold countdown at 2 remaining real resume cycles before any widening review.
   Why it matters:
   - The project wants evidence before widening the stamped active-surface set, and that decision should stay visible, countable, and review-triggering rather than becoming a vague reminder.
   Next action:
   - After each qualifying real resume cycle that uses the direct drift gate on live governed state and is judged acceptable, decrement the hold countdown in the governed todo or continuity surfaces and trigger a bounded widening review only when the countdown reaches 0.

3. Decide which additional `dcoir-*` skills truly need the same stateful flush/manicure and verbose-state treatment.
   Why it matters:
   - The tracker, enforcement, direct-resume, plan-tracker, and coordinated-trio campaign now cover the strongest first candidates, but the project still should not widen the pattern blindly.
   Next action:
   - After the coordinated trio campaign is validated, inventory the remaining stateful skills and decide which need the same pattern next.

4. Use canonical task-memory preflight before GitHub-family execution work.
   Why it matters:
   - Validated procedures should be consulted before friction, not after avoidable misses or retries.
   Next action:
   - Check knowledge/task_memory and the applicable GitHub procedures before repo writes, deletes, packaging, or structural changes.

5. Treat post-create and post-update helper-skill regression as mandatory.
   Why it matters:
   - Packaging success is not sufficient proof that a helper skill is ready for broader workflow use.
   Next action:
   - Pass every helper-skill create or update through dcoir-skill-regression-auditor and keep bounded evidence when full integrated regression is not yet practical.
