DCOIR Active Now

Current state id: 2026-04-04-hardcoded-audit-v2

1. Continue the hard-coded project variable audit on remaining eligible skills and repo-level workflow surfaces.
   Why it matters:
   - The project wants helper skills to evolve with current governed project structure instead of repeatedly breaking on moved files, renamed control surfaces, or transferred workflow context.
   Next action:
   - Continue replacing brittle assumptions with governed discovery-contract lookups where practical, and document any bounded exact-contract exceptions explicitly.

2. Keep the CI/CD-style validation regime active and use the discovery contract plus parity surfaces to anchor the broader helper-skill deep-dive and code-hygiene campaign.
   Why it matters:
   - The project now has enough grounded inventory, parity, and discovery structure that deeper testing should follow the real current scope rather than chat memory.
   Next action:
   - Use the validation regime and parity results to rank the next deep-dive targets after each audit pass lands.

3. Use the shared pre-push flush and staged-governed-update contract for session-memory-enabled or buffer-capable skills.
   Why it matters:
   - The project wants future governed pushes to lose only work since the last push, not buffered tracker or helper-memory state that should have landed in the same grouped update.
   Next action:
   - Before future governed pushes, require a bounded flush/manicure review for the stateful or GitHub-memory-backed skills that are active in the same branch.

4. Maintain the stamped active-surface widening hold countdown at 2 remaining real resume cycles before any widening review.
   Why it matters:
   - The project wants evidence before widening the stamped active-surface set, and synthetic or generated-artifact validation should not count as real live resume cycles.
   Next action:
   - Decrement the countdown only after a qualifying real resume cycle that uses the direct drift gate on live governed state and is judged acceptable.
