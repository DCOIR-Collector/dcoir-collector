DCOIR Queued Next

1. Add a governed workflow note for the validated GitHub operating process once the conduit decision is settled.
   Why it matters:
   - Future sessions should not have to rediscover how approval-gated chat-bound GitHub work is supposed to operate.
   Next action:
   - If @dcoir-github-api validates, document the operating process; if it does not, document the approved alternate path and explicit authorization rule.

2. Define a sustainable split standard for growth-prone governed files.
   Why it matters:
   - The project should not keep recreating oversized files that are difficult to update safely.
   Next action:
   - Define index-plus-child-file patterns, naming rules, and traceability requirements.

3. Continue maintaining and extending knowledge/task_memory as validated procedures, limitations, and failure signatures are learned.
   Why it matters:
   - The task-memory bank is now the durable GitHub memory location for validated lessons.
   Next action:
   - Keep promoting validated session lessons into canonical records and compiled indexes.
