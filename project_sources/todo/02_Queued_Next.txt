DCOIR Queued Next

1. Add governed workflow and navigation notes for the newly extracted readable source folders once the first review pass is complete.
   Why it matters:
   - Future sessions should not have to rediscover what was extracted, where it lives, and why it matters.
   Next action:
   - After the review pass, add stronger folder indexes, topic notes, and cross-links where the extracted material has durable project value.

2. Define a sustainable split standard for growth-prone governed files.
   Why it matters:
   - The project should not keep recreating oversized files that are difficult to update safely.
   Next action:
   - Define index-plus-child-file patterns, naming rules, and traceability requirements.

3. Audit the repo for true duplicate content and prepare the easiest automation-first removal path.
   Why it matters:
   - Duplicate readable material can create confusion and maintenance drift, and the operator prefers automated comparison and cleanup over manual review.
   Next action:
   - Compare actual content rather than names or paths, remove only verified duplicates, and if connector-only comparison is too cumbersome, define the simplest operator-friendly automated path with simple step-by-step instructions and minimal manual work.

4. Continue maintaining and extending knowledge/task_memory as validated procedures, limitations, and failure signatures are learned.
   Why it matters:
   - The task-memory bank is now the durable GitHub memory location for validated lessons.
   Next action:
   - Keep promoting validated session lessons into canonical records and compiled indexes.
