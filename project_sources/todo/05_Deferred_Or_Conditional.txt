DCOIR Deferred Or Conditional

1. After text-bearing zip contents are extracted and uploaded, update the repo structure and awareness files so the extracted readable sources become first-class project knowledge where appropriate.
   Why it matters:
   - Extracted readable text should be integrated deliberately, not left as loose side material.

2. Apply additional skill package maintenance only when a new validated bundle is ready and operator conditions allow.
   Why it matters:
   - Skill changes should remain bounded, regression-tested, and intentional.

3. If future GitHub procedures or failure signatures are validated, promote them into knowledge/task_memory and refresh the compiled indexes.
   Why it matters:
   - The canonical memory bank should keep improving as new validated workflows are learned.

4. If a new chat-bound GitHub execution surface, new connector, or Agent Mode path is proposed in the future, do not adopt it into the working line until it is explicitly approved and fully validated.
   Why it matters:
   - The project should not drift into unvalidated execution-path assumptions.

5. If the session-tracker workflow later proves that a dedicated session close-out skill is cleaner than the current embedded model, treat that as a bounded follow-on helper-skill decision rather than as an assumed immediate change.
   Why it matters:
   - The current update should validate the real workflow before introducing another helper-skill surface.

6. If the current-state and version-checking problem is later solved by a durable verification or enforcement mechanism, refresh the control-plane and continuity model to reflect that new authority posture explicitly.
   Why it matters:
   - The project should not keep carrying temporary drift-handling rules after a stronger solution is validated.

7. Keep the GitHub-native mirror pilot and its additive sync note explicitly tracked until the mirror issue or project review, Airtable follow-on, and any broader governed refresh decision are completed.
   Why it matters:
   - The additive sync note at project_sources/WD-20260413_GitHub_Mirror_Phase1_Parallel_Run_Sync_Note.txt is durable, but it can still be overlooked if it is not carried in the authoritative todo structure during the parallel-run pilot.
