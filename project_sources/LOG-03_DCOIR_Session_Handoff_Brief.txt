DCOIR Session Handoff Brief

Current state id: 2026-04-08-major-version-collector-and-gemini-completion-wave-v1

Stable baseline
- GitHub-primary readable-source model remains in force.
- Standalone remains the PP-01 through PP-07 assembly lane.
- Gemini now has a larger, more verbose major-version stored runtime source tree plus a maintained-knowledge-sync, validation, and compile path.
- Collector now has a maintained-source single-runtime compile path, a transport-safe delivery package path, and a bounded targeted-collection feature wave.

Open follow-through
- generate the major-version Gemini bundle through the associated GitHub Action
- generate the major-version collector delivery package through the associated GitHub Action
- carry the resulting bundle outputs into tomorrow's dedicated functionality test chat
- use the major-version functionality test plan during that chat
- after that, run the bounded duplicate-readable-surface cleanup
- later, decide whether a bounded workflow should regenerate `supporting_assets/DCOIR_Collector.zip` from the approved collector delivery package path
