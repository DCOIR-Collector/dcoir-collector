DCOIR GitHub Skill Source Policy Decision - 2026-04-03

Purpose
- Record the approved 2026-04-03 decision that settles whether all `dcoir-*` skill source should be stored in GitHub.
- Unblock the governed rollout of readable helper-skill source into the GitHub working repository.

Approved decision
- All `dcoir-*` skill source should be stored in GitHub.
- GitHub is the governed readable source-of-truth for DCOIR helper-skill source once the relevant source set is landed under the approved source root.
- Helper working-state continuity remains under `dcoir_skill_memory/` and is separate from skill-source authority.

Approved source root
- `dcoir_skills/`

Initial rollout batch
- `dcoir-memory-preflight`
- `dcoir-decision-policy`
- `dcoir-plan-tracker`
- `dcoir-session-tracker`
- `dcoir-skill-regression-auditor`

Workflow implications
- The earlier evaluation line about whether all `dcoir-*` skill source should move into GitHub is now settled for the DCOIR workflow.
- New or newly-governed DCOIR helper-skill source should be landed under `dcoir_skills/` using the approved layout standard.
- Once a DCOIR helper skill has governed source in GitHub, future readable-source maintenance should prefer GitHub as the durable source-of-truth.

Blocked-state communication rule
- When policy, authority, or governance materially blocks progress, report the block immediately.
- State what is blocked, why it is blocked, what change would unblock it, and the best available courses of action.

Implementation posture
- Use `project_sources/DOC-06_DCOIR_GitHub_Helper_Skill_Source_Layout_And_Rollout.txt` as the evergreen layout and rollout companion to this dated decision.
- Land the initial five-skill source batch under `dcoir_skills/`.
- Refresh broader continuity files in a later bounded grouped existing-file update.

Best next move
- Use the settled policy and approved source root to land the initial five-skill governed source batch under `dcoir_skills/`, then verify the written source set by readback.
