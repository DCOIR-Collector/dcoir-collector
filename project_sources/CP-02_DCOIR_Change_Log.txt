DCOIR Change Log

[CURRENT]
Version: 3.9.17
Change class: regression-auditor skill patch, codex-learning preference capture, and follow-on skill-maintenance sync

What changed
- Patched and repackaged the external `dcoir-skill-regression-auditor` skill bundle so its planner and fixture references explicitly support the current-control-plane narrative-manifest fixture and the campaign rule to test the tester first.
- Captured the operator preference that optional ChatGPT Codex work should be offered as step-by-step, click-by-click learning tasks, starting with basic usage before more advanced workflows.
- Added a documentation-lane todo to consider creating a dedicated `dcoir-readme-maintainer` skill for root and folder README maintenance.
- Updated helper regression memory and repo-side continuity surfaces so the next skill-maintenance step is replacing the patched regression-auditor package and then deep-scanning the remaining `dcoir-*` skills.
- Corrected the manifest version lag so CP-01 now catches up to the current control-plane state.

Why
- The project needs `dcoir-skill-regression-auditor` to be trustworthy before using it to evaluate the rest of the helper-skill fleet.
- The user explicitly wants optional Codex learning tasks presented in a beginner-friendly, click-by-click way when they are useful for project work.
- README maintenance has grown into a repeatable enough workflow that it should be tracked as a potential dedicated skill.
- The control plane should not drift across CP-01, CP-02, handoff, and helper memory while the helper-skill patch campaign is underway.

Validation notes
- The patched `dcoir-skill-regression-auditor` package passed `quick_validate.py`, its updated planner script executed successfully, and the skill was repackaged as a valid bundle.
- The helper regression memory now records the regression-auditor package as patched and packaged with the next action set to external replacement plus a short post-install smoke regression.
- The repo-side continuity files now reflect the Codex-learning preference and the new README-maintainer-skill todo.

[PRIOR]
Version: 3.9.16
Change class: knowledge-doc-maintainer scanner patch and local regression validation

What changed
- Patched `dcoir-knowledge-doc-maintainer/scripts/scan_project.py` in the external skill package so it can parse the current narrative `CP-01_DCOIR_Version_Manifest.txt` model instead of relying only on the older section-key format.
- Repackaged the skill after the scanner patch and removed the unnecessary `__pycache__` artifact from the bundled package.
- Re-ran package validation, the current-control-plane success fixture, the missing-control-plane failure fixture, and representative build-doc artifact verification.
- Updated helper regression memory and repo-side continuity surfaces so the next step is package replacement plus a quick post-install smoke regression.

Why
- The refreshed post-delete skill text was ahead of the scanner logic: the package validated and `build_knowledge_docs.py` worked, but `scan_project.py` still returned zero recognized governed readable sources on a current-control-plane fixture.
- Leaving that mismatch in place would cause future knowledge-doc maintenance to misread the current GitHub-primary control plane even though the instructions had already been refreshed.
- The patched package should now be treated as the candidate replacement bundle, with only a post-install smoke regression left before the workflow returns to broader DOC-04 architecture work.

Validation notes
- `quick_validate.py` returned a valid skill package.
- `build_knowledge_docs.py` emitted the expected `.md.txt` file and `supporting_knowledge_docs.zip` on a representative spec fixture.
- Before the patch, the success fixture returned `recognized_governed_github_readable_sources` count 0; after the patch, the same fixture returned count 15.
- The missing-control-plane fixture still failed clearly with `FileNotFoundError`, preserving the expected stop gate.
