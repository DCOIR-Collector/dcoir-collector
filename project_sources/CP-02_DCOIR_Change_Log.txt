DCOIR Change Log

[CURRENT]
Version: 3.9.42
Current state id: 2026-04-05-continuous-exec-and-session-tracker-cleanup-v1
Change class: continuous-execution cadence alignment and session-tracker post-push cleanup automation

What changed
- Added `project_sources/LOG-30_DCOIR_Continuous_Execution_Cadence_And_Session_Tracker_Post_Push_Cleanup_2026-04-05.txt` as the governed decision record for aligning the operator's low-interruption continuous-execution preference with the all-skills campaign and for repairing the next highest-value `dcoir-session-tracker` workflow gap.
- Updated `dcoir-session-tracker` so `mark-governed-written` now clears staged governed-update entries, staged todo actions, and matching post-push cleanup notes that still reference the promoted item, reducing manual tracker cleanup after grouped pushes.
- Added an explicit `clear-post-push-cleanup` command for bounded bulk cleanup when any cleanup notes remain after item-level promotion handling.
- Updated the `dcoir-session-tracker` local-state workflow guidance and session-buffer workflow guidance so post-push cleanup behavior now matches the repaired automation path.
- Refreshed the active continuity surfaces so the coordinated all-skills campaign explicitly carries the durable continuous-execution cadence rule and the repaired `dcoir-session-tracker` cleanup behavior.

Why
- The operator clarified that coordinated campaign work should continue until there is a real operator action, blocker, materially changed evidence state, or true decision requirement rather than pausing at intermediate milestones.
- The all-skills campaign identified a real workflow friction point in `dcoir-session-tracker`: promoted items could be marked governed-written, but related staged entries and cleanup notes still required separate manual clearing steps.
- The highest-value next move was to repair that buffer-capable workflow path and align the governed continuity surfaces in the same bounded update set.

Validation notes
- The repaired `dcoir-session-tracker` local-state path was retested on a fresh local state file.
- A representative pre-push review still stages todo actions and post-push cleanup as expected.
- After the grouped-push simulation, `mark-governed-written` now removes the related staged todo actions and matching cleanup notes for the promoted item.
- The next immediate branch is to continue the coordinated all-skills campaign into the next highest-value workflow-state skill pass while starting bounded package-cleanliness enforcement across the skill library.

[PRIOR]
Version: 3.9.41
Current state id: 2026-04-05-startup-validated-and-manual-bundle-fix-v1
Change class: startup-flow live validation completion and github desktop manual repo-update bundle mode fix

What changed
- Added `project_sources/LOG-29_DCOIR_GitHub_Desktop_Manual_Repo_Update_Bundle_Mode_Fix_2026-04-05.txt` as the governed decision record for diagnosing and repairing the missing GitHub Desktop manual repo-update bundle lane.
- Recorded that the updated `dcoir-session-resume` plus `dcoir-memory-preflight` first-turn bootstrap pair and resume-status fast path were proven in a real fresh-session workflow.
- Updated `dcoir-repo-packager` so it now supports `github-desktop-manual-update` mode, which packages only the requested affected repo-relative files or folders with no wrapper root and surfaces the suggested commit `Summary` in the report and response.
- Updated the packager references so the GitHub Desktop manual repo-update bundle class is explicitly documented alongside the existing repo and bootstrap bundle classes.
- Refreshed the active continuity and todo surfaces so startup-flow proof is no longer treated as pending work, the broader helper-skill deep-dive line is promoted, and the stamped active-surface widening hold countdown moves from 2 remaining real resume cycles to 1 remaining real resume cycle.

Why
- The operator confirmed that the new startup flow works in a real session, so the governed state needed to stop treating that validation as still pending.
- The operator expected ZIP deliverables for GitHub Desktop repo updates, but the recorded delivery preference and the actual repo-packager behavior had drifted apart.
- The highest-value fix was to repair the existing packaging skill and align the governed continuity surfaces rather than keep responding with copy-paste file-content patches during GitHub Desktop delivery sessions.

Validation notes
- The updated startup bootstrap pair and bounded resume-status fast path are now live-validated in a real session.
- The updated `dcoir-repo-packager` skill package passes local packaging validation.
- The repaired GitHub Desktop manual repo-update bundle mode was exercised against a repo-shaped working tree to emit the current affected-path bundle with no wrapper root.
- The next immediate branch is to continue the broader helper-skill deep-dive and code-hygiene campaign from the stronger validated startup and repaired delivery baseline.
