DCOIR Change Log

[CURRENT]
Version: 3.9.26
Current state id: 2026-04-04-plan-tracker-v1
Change class: plan-tracker verbose resume-state and flush/manicure upgrade with widening-hold countdown tracking

What changed
- Reviewed `dcoir-plan-tracker` as the next stateful-skill candidate after the tracker, enforcement, and direct-resume reference implementations.
- Updated the governed `dcoir-plan-tracker` skill instructions, file-layout guidance, session-buffer workflow reference, and deterministic local scripts so plan resume state can preserve richer operator-facing detail by default.
- Added explicit plan-level flush/manicure surfacing for buffered plan state, pending flush items, promotion candidates, remain-local notes, and one best next move.
- Added generic plan-level validation-counter support so deferred governance decisions can be tracked with an explicit remaining-count and trigger action instead of staying as vague prose.
- Added governed todo and continuity language for a stamped-surface widening hold countdown with 2 remaining real resume cycles before any widening review is reconsidered.
- Added `LOG-11_DCOIR_Plan_Tracker_Verbose_Resume_State_And_Widening_Hold_Workflow_Decision_2026-04-04.txt` as the governed decision record for this bounded workflow branch.

Why
- `dcoir-plan-tracker` already owns durable plan state, buffered writes, and resume breadcrumbs, so it was the strongest next place for terse or under-surfaced continuity to create operator friction.
- The project wanted the widening hold to be visible, countable, and review-triggering rather than a chat-only suggestion that could be forgotten.
- A plan-level validation counter is a more durable way to track deferred governance decisions than relying on prose reminders alone.

Validation notes
- Local bounded regression passed the deterministic `dcoir-plan-tracker` script path: initialize a plan, update it with verbose resume-state detail, render `05_resume_state.md`, and confirm the richer operator-facing state plus countdown rendering.
- The updated `dcoir-plan-tracker` skill package passed local skill packaging validation.
- Wider workflow validation of the patched plan-tracker path remains the next bounded branch.

[PRIOR]
Version: 3.9.25
Current state id: 2026-04-04-resume-gate-v1
Change class: session-resume direct drift-gate consumption and active-surface stamp correction

What changed
- Bound `dcoir-session-resume` directly to the `dcoir-source-authority-auditor` drift gate so resume now checks the shared active-surface `current_state_id` and the `CP-01` / `CP-02` version pair before trusting supporting continuity surfaces.
- Added `LOG-10_DCOIR_Session_Resume_Direct_Drift_Gate_Workflow_Decision_2026-04-04.txt` as the governed workflow decision record for the direct resume-gate integration.
- Corrected the missing shared `current_state_id` on `project_sources/LOG-01_DCOIR_Todo_Log.txt` so the full initial stamped active-surface set is now aligned again.
- Rolled the active continuity surface set to the new shared `current_state_id` `2026-04-04-resume-gate-v1` to mark the direct-resume integration line.
- Refreshed the active todo and continuity surfaces so they now point at bounded validation of the direct resume-gate path and the question of whether the stamped surface set should widen.

Why
- Fresh-session resume is one of the highest-risk places for plausible-but-stale continuity to slip through, so it should consume the same explicit drift gate rather than relying only on human interpretation of supporting notes.
- Follow-on verification of 3.9.24 exposed that `LOG-01_DCOIR_Todo_Log.txt` had not yet received the shared `current_state_id`, so the initial stamped active-surface set needed a corrective refresh before it could be treated as fully aligned.
- The project wanted one drift-logic source of truth rather than a weaker reimplementation inside the resume skill.

Validation notes
- Local bounded regression passed the deterministic `dcoir-source-authority-auditor` script path against the corrected active-surface set.
- The updated `dcoir-session-resume` skill package passed local skill packaging validation and the contract-path review for direct drift-gate consumption.
- Wider workflow validation of the direct resume-gate path remains the next bounded branch.
