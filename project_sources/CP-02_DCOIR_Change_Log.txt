DCOIR Change Log

[CURRENT]
Version: 3.9.61
Current state id: 2026-04-05-scheduled-health-check-residue-pattern-fix-v1
Change class: scheduled-health-check residue-pattern correction and final first-wave prove-out follow-through

What changed
- Fixed `.github/workflows/scheduled-health-check.yml` so the packaging-residue check uses safe non-regex path and extension checks instead of the invalid PowerShell regex pattern that caused the live workflow to fail before real validation work began.
- Added `project_sources/LOG-47_DCOIR_Scheduled_Health_Check_Residue_Pattern_Fix_2026-04-05.txt` as the governed record for the detected failure, the root cause, and the corrective patch.
- Updated the manifest and active continuity surfaces so the repo now honestly records that one final scheduled-health-check prove-out run is still required before treating the baseline as fully stabilized again.

Why
- The first scheduled-health-check correction moved the residue step ahead of Python syntax validation, but the residue matcher itself still used an invalid regex string.
- Live workflow evidence showed the run was now failing on an invalid pattern error, not on actual packaging residue.
- Correcting the matcher preserves the hygiene goal without creating a false failure before the representative retrieval suite can run.

Validation notes
- The live workflow run failed in `Check for packaging residue` with an invalid pattern error for the `__pycache__` matcher.
- The summary panel also showed no harness summary or artifacts, which is consistent with the workflow failing before the retrieval suite ran.
- This patch only corrects the residue matcher and does not widen the workflow scope.

[PRIOR]
Version: 3.9.59
Current state id: 2026-04-05-first-wave-github-automation-stabilized-and-follow-on-queue-v1
Change class: first-wave GitHub automation stabilization and approved follow-on implementation queue capture
