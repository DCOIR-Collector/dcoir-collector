DCOIR Change Log

[CURRENT]
Version: 3.9.45
Current state id: 2026-04-05-memory-preflight-reference-alignment-v1
Change class: memory-preflight coordinated-campaign and GitHub Desktop trigger alignment

What changed
- Added `project_sources/LOG-33_DCOIR_Memory_Preflight_Campaign_And_GitHub_Desktop_Alignment_2026-04-05.txt` as the governed decision record for repairing reference-layer drift inside `dcoir-memory-preflight`.
- Updated `dcoir-memory-preflight` so its reference maps now explicitly include coordinated multi-skill patch campaigns and GitHub Desktop manual repo-update or grouped governed-push deliveries as preflight families.
- Updated `dcoir-memory-preflight` so the reference layer now pairs GitHub Desktop manual repo-update deliveries with grouped transaction, post-write verification, and connector-lane selection records when those delivery decisions are in scope.
- Updated the memory-preflight flush-trigger guidance so GitHub Desktop pushes and manual repo-update deliveries are surfaced explicitly instead of being left implicit under generic GitHub writes.
- Refreshed the active continuity and todo surfaces so the workflow-state high-risk wave now includes the repaired memory-preflight reference layer.

Why
- The coordinated all-skills campaign exposed a real routing drift: the core `dcoir-memory-preflight` instructions already said coordinated campaigns and delivery-lane work should use preflight, but the bundled reference maps still omitted those families.
- That mismatch increased the risk that future preflight responses could skip the right canonical record family during GitHub Desktop delivery sessions or broad coordinated patch cycles.
- The highest-value bounded fix was to align the skill's reference layer to the already-governed execution posture instead of widening into unrelated helper-skill changes.

Validation notes
- The updated `dcoir-memory-preflight` skill package passes clean packaging validation.
- Reference-layer review confirms that coordinated campaigns, GitHub Desktop manual repo-update deliveries, and grouped governed pushes are now explicitly represented in the preflight family map, GitHub record map, and flush-trigger guidance.
- The next immediate branch is to continue the workflow-state high-risk wave from the now repaired memory-preflight routing baseline.

[PRIOR]
Version: 3.9.44
Current state id: 2026-04-05-plan-tracker-next-step-and-blocked-state-fix-v1
Change class: plan-tracker next-step advancement and blocked-state guidance fix

What changed
- Added `project_sources/LOG-32_DCOIR_Plan_Tracker_Next_Step_And_Blocked_State_Fix_2026-04-05.txt` as the governed decision record for the bounded `dcoir-plan-tracker` transition-guidance repair.
- Updated `dcoir-plan-tracker` so completing or skipping the active task now advances `next_recommended_action` and `resume_state.exact_resume_goal` to the next eligible task instead of leaving stale starter text in operator-facing resume state.
- Updated `dcoir-plan-tracker` so blocking the active task now surfaces blocked-state guidance in operator-facing resume state instead of leaving the same stale next-step wording that applied before the blocker.
- Refreshed the active continuity and todo surfaces so the workflow-state high-risk wave now carries the repaired `dcoir-plan-tracker` transition-guidance behavior.

Why
- The coordinated campaign deep-dive found a real operator-facing continuity defect: marking the active task `done`, `skipped`, or `blocked` could leave `next_recommended_action` and `resume_state.exact_resume_goal` stuck on stale starter text.
- That behavior weakened the very resume quality the plan tracker is supposed to protect in long-running DCOIR work.
- The highest-value bounded fix was to repair the plan-state transition logic and then carry the new behavior into the governed continuity surfaces.

Validation notes
- The updated `dcoir-plan-tracker` skill package passes clean packaging validation.
- Representative local plan-state tests confirm that `done`, `skipped`, and `blocked` transitions now update the operator-facing next-step and blocked-state guidance instead of leaving stale resume text behind.
- The next immediate branch is to continue the workflow-state high-risk wave with the next buffer-capable or routing-critical helper-skill pass.
