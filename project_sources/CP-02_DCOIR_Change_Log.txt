DCOIR Change Log

[CURRENT]
Version: 3.9.57
Current state id: 2026-04-05-first-wave-github-automation-baseline-v1
Change class: widening-review acceptance and first-wave GitHub automation baseline implementation

What changed
- Added `.github/workflows/validate-on-push.yml` as the automatic baseline validation workflow for push-driven repo validation with browser-readable summaries and uploaded run artifacts.
- Added `.github/workflows/manual-full-validation.yml` as the manual GitHub web UI trigger for deeper suite selection and browser-readable validation output.
- Added `.github/workflows/scheduled-health-check.yml` as the routine weekly repo-health workflow with residue checks, required-surface checks, representative regression, and browser-readable summaries.
- Added `.github/dependabot.yml` for explicit GitHub Actions reference update coverage while leaving package-ecosystem version-update coverage deferred until supported dependency manifests exist in the repo.
- Added `project_sources/LOG-44_DCOIR_First_Wave_GitHub_Automation_Baseline_2026-04-05.txt` as the governed decision record for the first-wave implementation details, including the harness path correction needed for the current collector zip location.
- Updated the manifest and active continuity surfaces to record that the widening review is now accepted, the remaining architecture or governance line is closed except for maintenance-only fixes, and the first-wave automation baseline is now part of the live governed working line.

Why
- The project wanted to move from strategy-only tracking into a real browser-first GitHub automation baseline that works in both work and home environments.
- The user explicitly approved the recommendation set, approved durable tracking, and then approved proceeding into actual implementation.
- Opening the first-wave baseline only after the widening review preserves the project rule that architecture closeout happens before the main line shifts to deliverables-adjacent repeatability work.

Validation notes
- The live repo search and current UI evidence showed no existing governed `.github` workflow baseline before this wave.
- The live repo dependency graph showed no supported package manifests, so Dependabot package-ecosystem coverage is not assumed current.
- The current test harness exists and is suitable for workflow use, but its default master zip path is not present in the live repo; this baseline therefore calls the harness with `-MasterZipPath ..\supporting_assets\DCOIR_Collector.zip`.
- Light branch or ruleset safety remains a manual GitHub UI follow-through item because it is not represented by readable repo files in this wave.

[PRIOR]
Version: 3.9.56
Current state id: 2026-04-05-github-automation-and-ai-enablement-tracking-v1
Change class: approved GitHub automation and AI enablement adoption tracking
