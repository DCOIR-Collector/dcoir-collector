DCOIR Change Log

[CURRENT]
Version: 3.9.24
Current state id: 2026-04-04-enforcement-v1
Change class: current-state/version-checking enforcement workflow definition and source-authority drift-gate upgrade

What changed
- Defined a governed current-state and version-checking enforcement workflow for the active DCOIR continuity surfaces instead of leaving drift handling as an informal expectation.
- Rolled out a shared `current_state_id` across the initial active surface set: `CP-01`, `CP-02`, `LOG-03`, `LOG-05`, `LOG-01_DCOIR_Todo_Log.txt`, `LOG-01_DCOIR_Todo_Index.txt`, and `todo/01_Active_Now.txt`.
- Added `LOG-09_DCOIR_Current_State_And_Version_Checking_Enforcement_Workflow_Decision_2026-04-04.txt` as the governed workflow decision record for the enforcement model.
- Upgraded the governed `dcoir-source-authority-auditor` skill instructions, audit rules, and deterministic script so it can compare the shared `current_state_id`, compare `CP-01` and `CP-02` version alignment, report exact drift, and stop or bound work appropriately.
- Refreshed the active todo and continuity surfaces so they now point at bounded validation of the enforcement model and the question of whether the same gate should be consumed directly by `dcoir-session-resume`.

Why
- The project needed a real coordination mechanism that can prove when the active continuity surfaces are on the same page instead of relying on hopeful interpretation of loosely aligned prose.
- Version alignment between `CP-01` and `CP-02` is necessary but not sufficient; the broader active continuity surface set also needs a shared machine-checkable signal.
- The operator wanted a stronger enforcement posture for governance drift, but that posture should still separate drift detection and explicit callout from actual governed-file remediation.

Validation notes
- Local bounded regression passed the deterministic `dcoir-source-authority-auditor` script path for an aligned active-surface fixture and the drift failure gate for an intentionally mismatched `current_state_id` fixture.
- The upgraded auditor is ready for bounded workflow validation as the first explicit drift gate on the active continuity surface set.

[PRIOR]
Version: 3.9.23
Change class: pre-GitHub-update flush/manicure workflow definition and verbose session-tracker upgrade

What changed
- Defined a bounded pre-GitHub-update flush/manicure workflow for stateful helper-skill session state, using `dcoir-session-tracker` as the reference implementation.
- Embedded startup-speed, fresh-session resume predictability, and governance-gap detection into the active workflow line as explicit acceptance-test concerns instead of leaving them as informal chat observations.
- Added a verbose tracker-entry standard so operator-facing tracker items now preserve fuller detail, rationale, carry-forward context, and promotion intent by default.
- Updated the governed `dcoir-session-tracker` skill instructions, local-state workflow reference, export schema guidance, and local scripts so the tracker can store and render richer item detail.
- Refreshed the active todo and continuity surfaces so they now point at post-patch validation of the tracker reference model and the linked current-state or version-checking enforcement problem.

Why
- New-session resume should be quick, predictable, and repeatable rather than dependent on manual reconstruction from terse continuity notes.
- The tracker needs richer stored and rendered state so the operator can understand what is being carried forward without guessing what a short summary means.
- Governance drift remains a critical project risk, so the flush/manicure workflow now explicitly surfaces promotion candidates, remain-local state, and the enforcement path that still needs to be finalized.

Validation notes
- Local tracker regression passed the bounded script path: initialize local state, upsert verbose items, inspect the real local JSON file, and render a markdown export with richer item detail.
- The patched tracker reference model is ready for bounded fresh-session resume and governance-gap regression next.
