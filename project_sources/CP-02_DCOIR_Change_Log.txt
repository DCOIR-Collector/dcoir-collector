DCOIR Change Log

[CURRENT]
Version: 4.0.3
Current state id: 2026-04-09-chunking-reconstruction-hotfix-v1
Change class: bounded chunking reconstruction hotfix for the collector synthetic validation fixture

What changed
- Added an exact-write helper to `project_sources/collector_parts/DCOIR_Collector.04B_Feature_Wave_Targeted_Collection.ps1` for the synthetic oversized source artifact and its emitted chunk files.
- Switched the synthetic source artifact and synthetic chunk artifacts away from the general text helper so the validation fixture no longer relies on PowerShell text-write behavior that can normalize chunk boundaries differently from the original source artifact.
- Kept the chunk manifest, chunk-path JSON, and broader collector output shaping path unchanged so the hotfix stays narrowly scoped to the failing reconstruction path.
- Added `project_sources/LOG-63_DCOIR_Chunking_Reconstruction_Hotfix_2026-04-09.txt` to record the root cause, fix posture, and required rerun wave.

Why
- `validate-on-push` failed in the new `FullRegression` path even though package build, runtime restore, and the chunking oversize suite itself were already passing.
- The failing assertion was specific to exact reconstruction of the synthetic oversized source artifact from the emitted chunk files.
- The safest bounded repair is to make the synthetic fixture write exact UTF-8 text for the source and chunk files rather than weakening the harness comparison.

Validation notes
- This change claims the collector-side reconstruction hotfix was added to the governed source line.
- It does not claim the repaired wave has already passed until `validate-on-push`, `manual-full-validation`, and the manual bundle workflows are rerun after this hotfix is committed.
- The hotfix is intentionally narrow and does not broaden or relax the harness acceptance criteria.
