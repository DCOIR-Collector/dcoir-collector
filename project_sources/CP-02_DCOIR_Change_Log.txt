DCOIR Change Log

[CURRENT]
Version: 3.9.18
Change class: github connector reference-pack intake and task-memory promotion

What changed
- Added a governed GitHub connector reference pack under `knowledge/github_connector_reference/`.
- Stored a distilled machine-readable connector inventory in `knowledge/github_connector_reference/github_connector_information.json` derived from the uploaded JSON reference set.
- Added `knowledge/github_connector_reference/GitHub_Connector_Execution_Guide.md` as the human-facing operating guide distilled from the uploaded TXT reference view and current DCOIR workflow needs.
- Added canonical GitHub task-memory procedure `GH-PROC-008` so the connector reference pack can be used through normal preflight and lane-selection reasoning.
- Updated the task-memory manifest and compiled fast lookup so `GH-PROC-008` is part of the canonical memory path.
- Refreshed control-plane, handoff, knowledge-index, and todo surfaces so the GitHub connector reference-pack promotion work becomes the next active workflow line.
- Preserved the existing `dcoir-readme-maintainer` skill idea and added follow-on design tracking for a `dcoir-plan-tracker` skill and for evaluating whether all `dcoir-*` skill source should live in GitHub under a one-source-of-truth model.

Why
- GitHub connector execution now materially shapes repo workflow speed, reliability, and recoverability.
- The uploaded connector references were rich enough to deserve governed storage rather than being left only in chat history.
- A distilled JSON inventory plus a human-facing guide is easier to maintain than two large raw snapshots while still preserving both machine and operator utility.
- Durable connector-execution lessons should flow into canonical task memory instead of remaining only in ad hoc notes.

Validation notes
- The uploaded connector reference set identified `GitHub Connector Information` with `total_functions` equal to `61`.
- The governed JSON file is a distilled derivative chosen for speed and maintainability; the TXT upload was used to drive the human-facing execution guide.
- `GH-PROC-008` is now present in both the canonical task-memory procedure path and the compiled fast lookup index.
- Control-plane and continuity files now reflect the connector-reference-pack workflow as the next active GitHub-heavy improvement line.

[PRIOR]
Version: 3.9.17
Change class: regression-auditor skill patch, codex-learning preference capture, and follow-on skill-maintenance sync

What changed
- Patched and repackaged the external `dcoir-skill-regression-auditor` skill bundle so its planner and fixture references explicitly support the current-control-plane narrative-manifest fixture and the campaign rule to test the tester first.
- Captured the operator preference that optional ChatGPT Codex work should be offered as step-by-step, click-by-click learning tasks, starting with basic usage before more advanced workflows.
- Added a documentation-lane todo to consider creating a dedicated `dcoir-readme-maintainer` skill for root and folder README maintenance.
- Updated helper regression memory and repo-side continuity surfaces so the next skill-maintenance step is replacing the patched regression-auditor package and then deep-scanning the remaining `dcoir-*` skills.
- Corrected the manifest version lag so CP-01 now catches up to the current control-plane state.

Why
- The project needs `dcoir-skill-regression-auditor` to be trustworthy before using it to evaluate the rest of the helper-skill fleet.
- The user explicitly wants optional Codex learning tasks presented in a beginner-friendly, click-by-click way when they are useful for project work.
- README maintenance has grown into a repeatable enough workflow that it should be tracked as a potential dedicated skill.
- The control plane should not drift across CP-01, CP-02, handoff, and helper memory while the helper-skill patch campaign is underway.

Validation notes
- The patched `dcoir-skill-regression-auditor` package passed `quick_validate.py`, its updated planner script executed successfully, and the skill was repackaged as a valid bundle.
- The helper regression memory now records the regression-auditor package as patched and packaged with the next action set to external replacement plus a short post-install smoke regression.
- The repo-side continuity files now reflect the Codex-learning preference and the new README-maintainer-skill todo.
