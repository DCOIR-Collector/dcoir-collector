[CURRENT]
Version: Validated_DCOIR_GitHub_Operator_Existing_File_Update_Lane_3_9_1
Type: Existing-file update validation, first-wave operator adoption, and governed workflow alignment
Summary:
- The connector-backed dcoir-github-operator now has a validated existing-file update lane that uses the GitHub connector low-level git-object path instead of the brittle high-level existing-file wrapper.
- Live project-chat validation proved the operator can create helper-memory files, update an existing file through the low-level lane, read back the result, remove the test marker, and verify cleanup on main.
- The high-level existing-file wrapper remains unsuitable for governed text modifications in this chat environment because overwrite calls require SHA-aware semantics that the exposed wrapper path does not handle reliably.
- The dcoir-github-operator skill package was refreshed to route new-file creates to the high-level create path and existing-file updates to the low-level git-object lane.
- Obsolete .env-style auth placeholder artifacts were removed from the skill package because the project runtime model is connector-backed rather than direct-API-first in chat.
- The dcoir-github-operator icon and metadata were refreshed so the skill more accurately represents its GitHub execution-layer role.
- First-wave dependent helper skills were refreshed to use dcoir-github-operator as the default GitHub execution layer for helper-memory continuity.
- Governing workflow and log files were refreshed so the live project workflow now describes the validated operator behavior instead of the earlier testing gate.

Validation summary:
- Direct connector create_file remains valid for bounded new-file creates.
- Existing-file update through the high-level wrapper reproduced the same blocker: overwrite semantics required SHA-aware handling that the exposed wrapper path did not complete reliably.
- The low-level git-object lane was validated on both branch-safe and direct-main workflows using create_blob, create_tree, create_commit, update_ref, and fetch_file readback.
- A live probe file in dcoir_skill_memory/dcoir-github-operator/ confirmed create, update, readback, cleanup, and final readback on main.
- The patched dcoir-github-operator package was then live-validated against the real repository using the formerly failing existing-file update case.

Operational intent:
- Keep dcoir-github-operator as the default GitHub execution layer for this project while aligning its write behavior to the validated connector reality.
- Preserve a clean operator workflow: high-level create for safe new files, low-level git-object lane for existing-file updates, connector readback after writes, and branch-safe use when risk is higher.
- Reduce future ambiguity about whether a skill-layer plan or the connector execution path should be trusted for governed text writes.
- Carry the validated operator behavior into dependent helper skills and evergreen workflow documentation.

[PRIOR]
Version: GitHub_Primary_Bootstrap_and_No_Duplicate_Readable_Sources_3_9_0
Type: Governance model change, GitHub-primary working-source shift, and Project bootstrap minimization
Summary:
- DCOIR now shifts to a GitHub-primary working-source model for readable governed text files.
- The repository malwaredevil/dcoir-collector is now the sole working source for readable text updates, review, and resume-side source access.
- Project space is no longer treated as a duplicate readable text repository. It is now the bootstrap and applied runtime anchor.
- Resume behavior now starts with Project Instructions and then uses the GitHub connector to read CP-01, CP-02, and the remaining governed readable source set from malwaredevil/dcoir-collector.
- The no-duplicate working-source rule is now explicit: do not keep duplicate editable readable text files in both GitHub and Project space.
- The collector and harness readable sources are now governed in GitHub native formats: DCOIR_Collector.ps1, run_DCOIR_Tests.ps1, and run_DCOIR_Tests.cmd.
- Knowledge markdown in knowledge/ is now the readable working source for knowledge maintenance when present; supporting_knowledge_docs.zip may still be retained as a delivery or reference asset.
- The settings baseline advances to project_settings/SET-06_AFRICOM_SOC_IR_Project_Instructions_v15.txt to make the GitHub-primary bootstrap rule durable in future resume flows.
- Skill updates remain a required follow-on work item after the GitHub-primary bootstrap model is applied.

Validation summary:
- GitHub native reads were directly validated for the real imported collector and harness files.
- The low-level GitHub direct-update path was validated and then used on main, proving that frequently changing governed text can be updated directly in the repository instead of via patch ZIPs.
- The repo inventory confirmed that extracted knowledge markdown exists alongside supporting assets, enabling a readable-source versus delivery-asset distinction.
- The earlier duplicate-heavy Project refresh approach was intentionally superseded because it preserved the exact source-location ambiguity the operator wanted removed.

Operational intent:
- Eliminate confusion about whether GitHub or Project space holds the current readable source.
- Preserve one readable working source for text while keeping Project space useful as a bootstrap/runtime anchor.
- Ensure future resume behavior knows where to go first without relying on hidden memory or ambiguous mirrors.
- Keep binary or infrequently changed assets separable from readable governed text.
- Make future skill updates and workflow updates target the GitHub-primary model instead of the older mirrored-text model.
