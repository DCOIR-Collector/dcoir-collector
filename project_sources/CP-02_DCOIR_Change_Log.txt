DCOIR Change Log

[CURRENT]
Version: 3.9.48
Current state id: 2026-04-05-collector-operator-github-primary-and-control-plane-pair-fix-v1
Change class: collector/operator github-primary alignment and control-plane version-pair correction

What changed
- Added `project_sources/LOG-35_DCOIR_Collector_Operator_GitHub_Primary_And_Control_Plane_Pair_Fix_2026-04-05.txt` as the governed decision record for the bounded collector/operator follow-through alignment pass across `dcoir-operator-workflow-hardener` and `dcoir-collector-qa`, together with a correction of the control-plane version-pair mismatch left in the previous live repo state.
- Updated `dcoir-operator-workflow-hardener` so it now uses GitHub-primary governed-readable-source language consistently instead of older uploaded-source wording when choosing operator execution lanes and reasoning from the current collector source.
- Updated `dcoir-collector-qa` so the current GitHub-readable collector and harness sources are the default QA scope, rollback material is treated as an explicit comparison or historical-regression lane rather than a primary default source, and the maintenance contract no longer treats a local harness CMD wrapper as a maintained default command block.
- Updated `dcoir-collector-qa` references so harness guidance points to the current readable harness source instead of older harness-mirror wording.
- Corrected the live control-plane version pair by bringing `CP-02` forward to match the already-live `CP-01` branch and then advancing both surfaces together in one bounded grouped update.

Why
- The coordinated campaign found that the collector-follow-through family still carried older uploaded-source or mirror-oriented wording even though the current DCOIR working line is GitHub-primary.
- The same review found a live control-plane defect: `CP-01` had already advanced to 3.9.47 while `CP-02` still reported 3.9.46, which would eventually undermine the source-authority drift gate if left uncorrected.
- Grouping the collector/operator wording fixes with the control-plane pair correction reduced operator friction and avoided surfacing a separate one-off patch just to repair the version mismatch.

Validation notes
- The updated `dcoir-operator-workflow-hardener` and `dcoir-collector-qa` skill packages pass clean packaging validation with no runtime-residue entries included.
- Reference review confirms that operator guidance and collector QA now point at the current GitHub-readable collector and harness sources, and that rollback material is treated as bounded historical comparison input rather than a default authoritative source.
- The next immediate branch is to continue the workflow-state, authority, and collector-follow-through high-risk wave from the now corrected control-plane version pair and GitHub-primary collector/operator baseline.

[PRIOR]
Version: 3.9.47
Current state id: 2026-04-05-resume-authority-bounded-assets-and-batched-prompts-v1
Change class: resume-skill packaging repair and source-authority bounded supporting-asset handling

What changed
- Added `project_sources/LOG-34_DCOIR_Resume_Packaging_And_Source_Authority_Bounded_Asset_Handling_2026-04-05.txt` as the governed decision record for the bounded resume/authority alignment pass across `dcoir-session-resume` and `dcoir-source-authority-auditor`.
- Repaired `dcoir-session-resume` so the skill packages cleanly with valid skill frontmatter and grouped follow-up prompt guidance that matches the current batched manual-update posture.
- Repaired `dcoir-source-authority-auditor` so missing supporting assets no longer trigger a hard-stop conflict unless the current task explicitly depends on those assets as authority.
- Preserved authoritative readable-source absence as a hard-stop conflict and kept the bounded-versus-hard-stop distinction explicit in both the audit rules and the deterministic audit script.
- Refreshed the active continuity surfaces so the coordinated campaign now carries valid resume packaging and bounded authority-audit asset handling together.

Why
- The coordinated campaign found that `dcoir-session-resume` still lacked normal skill frontmatter even though it is part of the mandatory first-turn bootstrap pair.
- The same review found a contradiction in `dcoir-source-authority-auditor`: the skill said supporting assets are not control-plane authority, but the deterministic audit script could still hard-stop when only a supporting asset was absent from the workspace.
- Those two issues sit on the same resume/authority bootstrap lane, so repairing them together reduced friction for current-state checks in partial or batched delivery workspaces.

Validation notes
- The updated `dcoir-session-resume` and `dcoir-source-authority-auditor` skill packages pass clean packaging validation.
- Representative deterministic audit tests confirm that missing authoritative readable sources still hard-stop, while missing supporting assets now produce a bounded outcome when the task does not depend on them as authority.
- The next immediate branch is to continue the coordinated campaign into the next workflow-state or authority-adjacent helper-skill pass while keeping compatible fixes for the next meaningful grouped manual update/install wave.
