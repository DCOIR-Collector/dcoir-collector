DCOIR Change Log

[CURRENT]
Version: 3.9.8
Change class: task-memory preflight expansion, grouped GitHub transaction rule, and post-alignment state refresh

What changed
- Added canonical GitHub task-memory procedures for grouped multi-file transactions and memory-preflight routing.
- Refreshed the task-memory manifest and compiled fast lookup so the new GitHub procedures are first-class retrievable records.
- Updated DOC-01 so GitHub-family execution work now consults canonical task memory first and prefers one bounded multi-file git-object transaction for related existing-file edits or deletions.
- Refreshed CP-01, LOG-01, LOG-03, the todo index, and key split todo lanes so the current work line no longer points at the completed helper-skill audit or the retired dcoir-github-operator lane.
- Prepared a dedicated dcoir-memory-preflight skill and an updated dcoir-decision-policy skill package so the preflight behavior can be applied in the skill layer as well as in governed docs and task memory.

Why
- GitHub-family work should not require operator reminders before canonical task memory is consulted.
- Related repo changes should land as one bounded transaction instead of one-file-at-a-time churn when they belong together.
- The control plane and split todo structure needed to catch up to the current post-skill-alignment project state so the next move points back to the real deliverables and remaining todo work.

Validation notes
- The new task-memory records are present under knowledge/task_memory/10_domains/github/procedures/ and indexed in the compiled fast lookup.
- DOC-01, CP-01, LOG-01, LOG-03, the todo index, and the refreshed split todo lanes are updated together in one grouped change set.
- The grouped git-object transaction lane remains the preferred path for related existing-file repo edits and deletions.

[PRIOR]
Version: 3.9.7
Change class: repo text refresh, split todo structure activation, and connector memory refinement

What changed
- README.md was expanded into a stronger repo entry point with project mission, deliverables, scope priorities, navigation, and documentation direction.
- The todo line now includes a split structure consisting of project_sources/LOG-01_DCOIR_Todo_Index.txt plus project_sources/todo/*.txt child files.
- LOG-01 and LOG-03 were refreshed to point to the split todo structure and the clarified deliverable set.
- CP-01, DOC-01, and DOC-03 were refreshed so the control plane and layout docs acknowledge the split todo structure and the stronger repo-guide posture.
- The GitHub task-memory procedure for existing-file updates was refined to record that compare_commits with base=main and head=main can recover the current head sha when fetch_commit is flaky upstream.

Why
- The repo needed a clearer top-level statement of what the project is and what it delivers.
- The todo line needed a sustainable split model rather than one indefinitely growing file.
- The control-plane and layout docs needed to stay aligned with the new live repo structure.
- The connector-first GitHub write lane needed a more resilient documented head-sha recovery method.

Validation notes
- README.md, LOG-01, LOG-03, CP-01, DOC-01, DOC-03, and the split todo files are live on main.
- The low-level existing-file update lane was revalidated on main through create_blob, create_tree, create_commit, update_ref, and readback verification.
- compare_commits base=main and head=main successfully returned the current head sha during the write flow.
- No live repo text hits were found for dcoir-github-operator or DCOIR_Collector_parts during the repo-side cleanup scan.
