DCOIR Change Log

[CURRENT]
Version: 3.9.30
Current state id: 2026-04-04-validation-contract-v1
Change class: validation-regime branch activation and session-memory pre-push flush contract

What changed
- Added `LOG-15_DCOIR_Validation_Regime_And_Session_Memory_Pre_Push_Contract_Decision_2026-04-04.txt` as the governed decision record for the next validation-regime branch and the shared pre-push flush contract.
- Activated `dcoir-validation-orchestrator` as the owner for the next CI/CD-style end-to-end and edge-case validation regime built from the inventory and gap-analysis scope map.
- Updated `dcoir-validation-orchestrator` so it can emit validation plans for end-to-end workflows, edge cases, skill deep dives, documentation alignment, packager validation, and the session-memory pre-push contract.
- Updated `dcoir-session-tracker` so it now supports explicit staged-governed-update tracking, pre-push flush output, and post-push cleanup tracking instead of relying only on informal chat carry-forward.
- Updated the other buffer-capable or GitHub-memory-backed skills so their flush-check guidance now requires pre-push staging and same-branch governed updates when a suitable grouped push is already happening.
- Updated `dcoir-decision-policy` so multi-skill delivery now prefers one outer zip with top-level per-skill zips named after the live skill names when more than one updated skill package is being handed to the operator.
- Refreshed the active todo and continuity surfaces so they now point at the validation-regime and session-memory-contract branch while keeping the widening-hold countdown at 2 real resume cycles.

Why
- The inventory pass and recent grouped pushes showed that validation planning and session-memory staging were lagging behind the now-broader governed workflow.
- The operator explicitly wants the next governed push to lose only work since the last push, not buffered tracker or helper-memory state that should have been included in the same grouped update.
- The operator also clarified a more specific multi-skill delivery preference for one outer zip containing top-level per-skill zips named after the live skills.

Validation notes
- Bounded source validation confirms the repaired repo-packager now follows the current repo-style bundle model and current update-mode bootstrap roots.
- Local regression passed the updated `dcoir-validation-orchestrator` plan emission path and the updated `dcoir-session-tracker` staged-governed-update path.
- Local skill packaging validation passed for the changed skills in this batch.
- Wider use of the validation regime and the pre-push flush contract remains the next governed branch.

[PRIOR]
Version: 3.9.29
Current state id: 2026-04-04-inventory-pass1-v1
Change class: inventory and documentation alignment pass one plus repo-packager current-project repair
