DCOIR Change Log

[CURRENT]
Version: 3.9.60
Current state id: 2026-04-05-scheduled-health-check-residue-order-fix-v1
Change class: scheduled-health-check self-generated residue fix and follow-through correction

What changed
- Reordered `.github/workflows/scheduled-health-check.yml` so the packaging-residue check runs before Python syntax validation.
- Added `project_sources/LOG-47_DCOIR_Scheduled_Health_Check_Residue_Order_Fix_2026-04-05.txt` as the governed record of the newly discovered workflow self-conflict and the focused correction.
- Updated the manifest and active continuity surfaces to replace the stale "stable baseline, nothing immediate required" posture with the accurate next move: apply the focused scheduled-health-check fix and rerun that workflow once.

Why
- Live manual execution of scheduled-health-check showed that the workflow was failing on a `.pyc` file generated by its own earlier Python syntax-validation step rather than on pre-existing committed residue.
- That makes the failure a workflow-ordering bug, not evidence that the governed repo currently contains packaged residue.
- A bounded fix is enough: move the residue scan earlier, then rerun the workflow once to restore the stable baseline posture honestly.

Validation notes
- The live failure occurred in the packaging-residue step and referenced a generated `__pycache__/render_attention_signal.cpython-311.pyc` path.
- The same workflow already has the corrected action versions and hardened non-fatal browser-summary step, so the remaining defect is the self-generated residue ordering issue.
- This wave does not widen the platform-adoption scope; it is a focused corrective patch to finish the first-wave automation proof set honestly.

[PRIOR]
Version: 3.9.59
Current state id: 2026-04-05-first-wave-github-automation-stabilized-and-follow-on-queue-v1
Change class: first-wave GitHub automation stabilization and approved follow-on implementation queue capture
