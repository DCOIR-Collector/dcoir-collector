apiVersion: v1
kind: Pod
metadata:
  name: privileged-probe
spec:
  containers:
    - name: worker
      image: alpine:latest
      securityContext:
        privileged: true
      volumeMounts:
        - mountPath: /host
          name: host-root
  volumes:
    - name: host-root
      hostPath:
        path: /
