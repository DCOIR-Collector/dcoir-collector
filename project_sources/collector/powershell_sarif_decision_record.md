# PowerShell SARIF Decision Record

- Schema: `dcoir_powershell_sarif_decision_record_v1`
- Issue: `#269`
- Parent issue: `#260`
- Decision: `defer_sarif_upload`
- Decision date: `2026-06-17`
- Validation: `pass`

## Scope

This is a decision record only. #269 does not generate SARIF, upload SARIF, edit workflows, enable code scanning, change required checks, change artifact retention, or change alert ownership.

## Decision

Defer SARIF/code-scanning upload for the current #260 PowerShell validation series.

The current JSON and Markdown review-assist artifacts are sufficient for this series because they expose source reports, normalized findings, inventory context, baseline and suppression state, engine/Pester boundaries, missing optional analyzer evidence, unclaimed artifacts, warnings, and explicit non-claims without creating GitHub security alerts.

## Evidence

| Issue | Evidence | Decision relevance |
| --- | --- | --- |
| `#266` | `22` findings, `22` classified, `0` unclassified, `0` baselines, `0` suppressions, all current findings classified as `advisory` | Governance is valid for fixture/custom-check evidence, but there is no live analyzer artifact to upload as SARIF. |
| `#267` | Engine/Pester boundary passes, but PowerShell 7 analyzer evidence and Windows PowerShell 5.1 runtime evidence remain future or unclaimed | Code-scanning alerts would overstate current proof if uploaded now. |
| `#268` | Review-assist report passes with `7` required reports present, `22` normalized findings, `1` optional source report missing, `10` carried-forward warnings, `3` unclaimed artifacts, and `9` non-claims | JSON/Markdown artifacts are the intended current reviewer handoff and explicitly do not claim SARIF, workflow, code-scanning, or required-check readiness. |

## Defer Rationale

- No validated committed live PSScriptAnalyzer report is present for #262.
- The #268 report is local committed JSON/Markdown evidence and does not claim SARIF generation or upload readiness.
- The #267 boundary leaves PowerShell 7 analyzer evidence and Windows PowerShell 5.1 runtime evidence as unclaimed or future evidence.
- Uploading current fixture/custom-check evidence as code-scanning alerts would risk noisy or misleading alert ownership before fingerprints, severity mapping, event restrictions, and alert lifecycle are designed.
- Parent #260 can continue through workflow/local integration planning using fail-closed JSON/Markdown report generation without requiring SARIF.

## Future SARIF Gate

Any later SARIF implementation requires a separate current-session operator approval and must define:

- SARIF producer and source reports consumed.
- SARIF 2.1.0 compatibility proof.
- Stable rule IDs, rule metadata, severity mapping, help text, and fix text.
- Stable fingerprints or partial fingerprints that avoid alert churn.
- Generated SARIF artifact names and readback path.
- Minimal workflow permissions, including any `security-events: write` requirement and why the event context is safe.
- Pull request fork/private-repo behavior and explicit `pull_request_target` avoidance or justification.
- Code-scanning alert ownership, triage expectations, and noise-control policy.
- Upload run evidence plus separate readback of actual code-scanning alerts before claiming alert quality.

## Non-Claims

- No SARIF file is generated by #269.
- No SARIF upload is performed by #269.
- No GitHub workflow file is changed by #269.
- No code-scanning alert, code-scanning enablement, or required-check behavior is changed by #269.
- No `pull_request_target` workflow design is introduced by #269.
- No artifact-retention setting is changed by #269.
- No live PSScriptAnalyzer evidence is claimed when the #262 analyzer report is absent.
- No parent #260 closeability or #270 workflow-readiness claim is made by #269.

## Handoff To #270

Because SARIF upload is deferred, #270 should plan workflow/local integration around the existing fail-closed JSON/Markdown report path unless a future approval changes this decision.
