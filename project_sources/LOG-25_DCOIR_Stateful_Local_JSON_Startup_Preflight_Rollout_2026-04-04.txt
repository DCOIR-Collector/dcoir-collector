DCOIR Stateful Local JSON Startup Preflight Rollout - 2026-04-04

Current state id: 2026-04-04-session-start-bootstrap-and-prompt-review-v1

Purpose
- This log records the bounded rollout of startup local-JSON presence preflights and explicit missing-state warnings for the DCOIR skills that currently use real local JSON working-state files.

Decision summary
- Treat only the DCOIR skills with real local JSON working-state files as in scope for this rollout instead of touching every stateful skill indiscriminately.
- The confirmed in-scope skills for this pass are `dcoir-session-tracker` and `dcoir-plan-tracker`.
- At the beginning of each new session that uses one of those skills, run that skill's local JSON presence preflight before other substantive local-state actions.
- For `dcoir-session-tracker`, keep `scripts/session_state_store.py ensure-state` as the startup preflight for `/mnt/data/dcoir_session_tracker/session_state.json`.
- For `dcoir-plan-tracker`, add `scripts/ensure_plan_state.py` as the startup preflight for each local plan folder `plan_state.json` working mirror.
- When a local JSON file is missing where one was expected, surface that plainly and do not silently treat the missing interval as uninterrupted file-backed continuity.
- For `dcoir-plan-tracker`, block local plan mutations that depend on `plan_state.json` until the local-state preflight has either proven the file exists or intentionally initialized a brand-new local plan folder.

What changed in this pass
- Updated `dcoir-session-tracker` instructions and local-state references so `ensure-state` is framed as the required beginning-of-session preflight for every new session that uses the skill.
- Added `dcoir-plan-tracker/scripts/ensure_plan_state.py` so local plan folders can prove whether `plan_state.json` is present and inspectable or, when appropriate, initialize a brand-new local plan-state file explicitly.
- Updated `dcoir-plan-tracker/scripts/init_plan.py` so brand-new local plan creation reports the local plan-state initialization result explicitly.
- Updated `dcoir-plan-tracker/scripts/add_task.py` and `dcoir-plan-tracker/scripts/update_plan_state.py` so they no longer proceed silently when an expected local `plan_state.json` file is missing.
- Added and refreshed `dcoir-plan-tracker` references so startup local plan-state preflight is part of the documented workflow, command surface, and local proof rules.

Why it matters
- The operator wants startup local-state proof, not only later-stage flush behavior, whenever a DCOIR skill depends on a real local JSON working-state file.
- The operator does not want silent local-state absence to create false confidence that continuity remained file-backed.
- The operator also does not want an over-broad rollout that touches skills which do not actually use a real local JSON working-state file.

Validation boundary
- Local bounded regression passed the new `dcoir-plan-tracker` startup local-state preflight path for existing local plan-state proof, brand-new local plan creation, and explicit missing-state warnings for expected local plan folders.
- Updated `dcoir-session-tracker` and `dcoir-plan-tracker` skill packages passed local packaging validation.
- The next step is a live workflow validation of the startup local-state preflight behavior during the next suitable governed push or plan-tracker local-plan branch.

Next immediate move
- Use the updated `dcoir-session-tracker` and `dcoir-plan-tracker` startup preflights in the next real workflow branch and confirm that the operator-visible presence or missing-state messaging is strong enough in practice.
