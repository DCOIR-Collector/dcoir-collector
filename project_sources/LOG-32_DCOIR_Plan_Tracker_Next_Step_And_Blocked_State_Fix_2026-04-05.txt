DCOIR Plan-Tracker Next-Step And Blocked-State Fix - 2026-04-05

Current state id: 2026-04-05-plan-tracker-next-step-and-blocked-state-fix-v1

Purpose
- Record the diagnosis and repair for the bounded `dcoir-plan-tracker` task-transition defect discovered during the coordinated all-skills workflow-state high-risk wave.

Decision summary
- Treat stale operator-facing next-step guidance after plan-task transitions as a real workflow-state defect, not a cosmetic formatting issue.
- Repair `dcoir-plan-tracker` so `done` and `skipped` transitions advance `next_recommended_action` and `resume_state.exact_resume_goal` to the next eligible task instead of leaving starter text in place.
- Repair `dcoir-plan-tracker` so blocked active-task transitions promote explicit blocker-resolution guidance into the operator-facing resume state.
- Keep the repair bounded to local plan-state transition rendering and continuity-surface refresh rather than broadening the pass to unrelated plan-tracker features.

Why it matters
- The operator uses plan-tracker state as a real resume aid during complex DCOIR work, so stale starter text after a completed task can misroute the next action even when the execution table is otherwise correct.
- Blocked-state transitions should point the operator at the blocker-resolution path instead of preserving an outdated generic next step.
- The workflow-state high-risk wave is specifically meant to catch these state-carry and resume-quality defects before they propagate into later governed pushes.

Validation notes
- Representative local plan flows confirmed that after `done` or `skipped` transitions the next eligible task is now promoted into `next_recommended_action` and `resume_state.exact_resume_goal`.
- Representative blocked-task flows confirmed that blocked-state mitigation guidance is now promoted into the operator-facing resume state.
- The updated `dcoir-plan-tracker` skill package passes clean packaging validation with no `__pycache__/`, `*.pyc`, or `.DS_Store` entries present.
- The current continuity surfaces were refreshed in the same change set so the coordinated campaign now treats the repaired `dcoir-plan-tracker` transition path as part of the active workflow-state baseline.

Next immediate move
- Continue the workflow-state high-risk wave after the repaired `dcoir-plan-tracker` transition path while carrying the bounded package-cleanliness enforcement line in parallel.
