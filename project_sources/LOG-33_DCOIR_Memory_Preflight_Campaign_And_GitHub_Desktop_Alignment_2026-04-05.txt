DCOIR Memory Preflight Campaign And GitHub Desktop Alignment - 2026-04-05

Current state id: 2026-04-05-memory-preflight-reference-alignment-v1

Purpose
- Record the diagnosis and repair for reference-layer drift inside `dcoir-memory-preflight` so the skill's bundled maps and flush-trigger guidance stay aligned to the already-governed coordinated-campaign and GitHub Desktop delivery posture.

Decision summary
- Treat the omission of coordinated multi-skill campaigns and GitHub Desktop manual repo-update or grouped governed-push deliveries from the memory-preflight reference layer as a real routing defect, not as a wording-only gap.
- Update `references/preflight_task_families.md` so coordinated campaigns and GitHub Desktop manual repo-update or grouped governed-push deliveries are explicit preflight families.
- Update `references/github_memory_query_map.md` so GitHub Desktop manual repo-update deliveries or grouped governed pushes explicitly pair `GH-PROC-006` with `GH-PROC-005` and include `GH-PROC-008` when connector-lane or delivery-shape selection remains in doubt.
- Update `references/session_buffer_flush_triggers.md` so GitHub Desktop pushes and manual repo-update deliveries are explicit flush-check triggers instead of being left implicit under generic GitHub writes.
- Update the main `SKILL.md` guidance so the core workflow and the bundled references now point at the same governed routing posture.

Why it matters
- The core `dcoir-memory-preflight` instructions already required preflight for coordinated multi-skill patch campaigns and GitHub-family delivery work, but the bundled reference files did not fully represent those families.
- That mismatch increased the risk that future preflight responses could skip the right canonical record family or fail to surface the correct grouped-push flush trigger during GitHub Desktop delivery sessions.
- The highest-value bounded fix was to align the skill's reference layer to the already-settled governed workflow rather than widening into unrelated helper-skill changes.

Validation notes
- The updated `dcoir-memory-preflight` skill package passes clean packaging validation.
- Reference-layer review confirms that coordinated campaigns, GitHub Desktop manual repo-update deliveries, and grouped governed pushes are now explicit in the preflight family map, GitHub record map, and flush-trigger guidance.
- The active continuity surfaces were refreshed in the same change set so the workflow-state high-risk wave now includes the repaired memory-preflight routing layer.

Next immediate move
- Continue the workflow-state high-risk wave from the repaired `dcoir-memory-preflight` routing baseline while keeping the package-cleanliness enforcement pass active across the helper-skill library.
