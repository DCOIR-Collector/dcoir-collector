DCOIR Standalone Prompt Full-Mission Refresh And PP-08 Regeneration - 2026-04-06

Current state id: 2026-04-06-standalone-prompt-full-mission-refresh-v1

Purpose
- Refresh the standalone analyst-facing prompt-pack authority line so it matches the full project mission rather than reading as collector-output-first only.
- Regenerate PP-08 from the refreshed modular source line.
- Record that the Gemini design line already remains structurally aligned closely enough that no immediate structural rewrite is required.

What was changed
- PP-01 through PP-07 were refreshed so the standalone analyst-facing prompt line now begins cleanly from Elastic alert triage as well as DCOIR collector output.
- The refreshed modular line now includes explicit triage-to-DCOIR escalation language, explicit next-expected-evidence language, and stronger authoritative IOC-enrichment handling.
- PP-02 now includes an alert_triage workflow mode plus escalation outcomes such as dcoir_collection, dcoir_targeted_enrichment, and ioc_enrichment.
- PP-08 was regenerated from the refreshed modular prompt-pack line so the runtime parity artifact now reflects the full alert-triage-to-DCOIR mission.

Why it matters
- The project mission and Gemini design line already covered Elastic alert triage plus DCOIR escalation, but the standalone prompt line still understated that scope and therefore risked drifting away from the real deliverable intent.
- Without refreshing the modular authority first, PP-08 would continue to lag the real project mission.
- Capturing the refresh durably prevents later sessions from rediscovering the same standalone prompt gap.

Validation notes
- The refreshed modular line now explicitly covers alert triage, explicit escalation into DCOIR collection or targeted enrichment, collector-output review, authoritative IOC enrichment, retrieved-artifact review, and final synthesis.
- PP-08 was regenerated from the refreshed PP-01 through PP-07 line.
- A bounded spot-check against PP-09, PP-10, and the generated Gemini markdown surface indicates that the merged Gemini parent-plus-seven-sub-agent design already matches the refreshed standalone mission closely enough that no immediate structural Gemini rewrite is required.

What this does not change
- The first-wave GitHub automation baseline remains in stable service.
- The work-safe browser-first operating constraint remains in force.
- GitHub Desktop manual grouped repo-update delivery remains the default governed write posture.
- A later bounded Gemini wording pass is still allowed if live-use testing proves one is useful.

Best next move
- Use the refreshed standalone prompt-pack line as current authority.
- Keep PP-08 regenerated whenever PP-01 through PP-07 change.
- Reserve a later bounded Gemini wording pass for only those areas that live-use testing proves still need refinement.
