DCOIR Coordinated Stateful Candidate Trio Campaign Decision - 2026-04-04

Purpose
- Record the approved 2026-04-04 workflow decision to patch the remaining highest-value stateful or continuity-sensitive helper-skill candidates in one coordinated campaign rather than a slow trickle of one-off pushes.
- Capture the operator-approved preference to prefer a bounded grouped push when the remaining similar scope is already known and regression can still remain explicit.

Approved durable workflow decisions
- The coordinated campaign set for this pass is:
  - `dcoir-decision-policy`
  - `dcoir-memory-preflight`
  - `dcoir-skill-regression-auditor`
- This campaign is intentionally narrower than "all remaining skills". It covers the strongest remaining stateful or workflow-control candidates after the tracker, enforcement, direct-resume, and plan-tracker reference implementations.
- When the remaining similar scope is already known and regression can still stay explicit and operator-understandable, prefer one bounded coordinated campaign and one grouped repo batch over a slow stream of onesy-twosey pushes.
- Each changed skill in a coordinated campaign must still receive explicit regression coverage; grouped delivery does not relax per-skill validation.
- `dcoir-skill-regression-auditor` should still validate itself first when it is in the changed set.
- The stamped active-surface widening hold remains at 2 remaining real resume cycles. This coordinated skill campaign is not itself a qualifying real resume cycle and must not decrement that hold.

Campaign implementation goals
- `dcoir-decision-policy`: surface buffered decision state, coordinated campaign scope, and deferred-review countdowns more explicitly, and persist the coordinated-batch preference as an approved durable rule.
- `dcoir-memory-preflight`: surface post-blocker promotion candidates with richer operator-facing detail and explicit flush-trigger handling.
- `dcoir-skill-regression-auditor`: plan coordinated multi-skill regression with explicit self-first sequencing, grouped readiness reporting, and more visible buffered regression state.

Best next move
- Run bounded regression on the coordinated stateful-candidate trio campaign, then decide which remaining stateful skills actually need the same pattern next instead of widening the campaign blindly.
