DCOIR Todo Index

Current state id: 2026-04-05-scheduled-health-check-residue-cleanup-v1

Current priority summary
1. Widening review remains accepted and the remaining architecture or governance line remains closed except for maintenance-only fixes.
2. Validate-on-push, manual full-validation, FullRegression, light ruleset safety, and Dependabot GitHub Actions flow are already proven.
3. One focused remaining corrective step is still required: remove the real committed packaging residue file or files and rerun `scheduled-health-check` once.
4. The root `.gitignore` residue guards should land as part of that cleanup.
5. After the rerun passes, return to the stable baseline posture and keep the main project line on deliverables.
6. Preserve the later approved platform-adoption items as queued bounded work in the earlier remembered order.
7. Keep package-cleanliness enforcement, parity verification, and representative regression active for future maintenance-only helper-skill fixes.

Current best next move
- Remove the confirmed committed residue file or files now, apply the root `.gitignore`, rerun `scheduled-health-check` once, and only then treat the first-wave automation baseline as fully stabilized again.
