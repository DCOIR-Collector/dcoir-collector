DCOIR Plan-Tracker Exact Resume Goal Autofill Fix And Validation Refresh - 2026-04-04

Purpose
- Record the approved 2026-04-04 workflow decision to repair the `dcoir-plan-tracker` exact-resume-goal auto-population defect discovered during bounded real-world validation.
- Keep the widening-hold countdown explicit while correcting the one remaining operator-facing gap in the patched plan-tracker path.

Approved durable workflow decisions
- `dcoir-plan-tracker` should not leave `resume_state.exact_resume_goal` empty when an active task has a usable `next_action` or the plan has a usable `next_recommended_action`.
- When a task transitions to `in_progress`, the skill should auto-populate `resume_state.exact_resume_goal` from the strongest available next-action field if the resume-goal field is still blank.
- A bounded generated-plan validation path is sufficient to prove this narrow repair, but it does not count as a qualifying real resume cycle for decrementing the stamped-surface widening hold countdown.
- The stamped active-surface widening hold remains at 2 remaining real resume cycles until qualifying live direct-resume cycles occur.

Repair validation sequence
1. Generate a fresh local plan path.
2. Add a task with an explicit `next_action`.
3. Transition that task to `in_progress` without manually setting `exact_resume_goal`.
4. Confirm that `05_resume_state.md` now renders a non-empty `exact_resume_goal`.
5. Confirm that the widening-hold countdown still renders and remains unchanged.

Best next move
- Treat the repaired `dcoir-plan-tracker` path as ready for bounded workflow use, keep the widening-hold countdown at 2 remaining real resume cycles, and then move to the next stateful-skill candidate review.
