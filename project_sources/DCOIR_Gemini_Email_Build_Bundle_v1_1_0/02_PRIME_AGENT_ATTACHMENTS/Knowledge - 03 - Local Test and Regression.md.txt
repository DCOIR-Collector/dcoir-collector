# Knowledge - 03 - Local Test and Regression

_How the local harness works and when to use each suite_

**Legacy-bundle status:** This attachment copy lives inside the retained `DCOIR_Gemini_Email_Build_Bundle_v1_1_0` legacy reference bundle. It is preserved for historical context and should not override current governed sources.

**Current source rule:** Use the current maintained knowledge files and the current control plane for live instructions. For harness authority, use `project_sources/run_DCOIR_Tests.ps1` when retained and current.

**Summary:** The local harness tests the stable collector line from a local repo-style layout without changing the collector engine for every test case.

| Source class | Current authoritative basis |
| --- | --- |
| Project sources | `project_sources/run_DCOIR_Tests.ps1`; `project_sources/DOC-01_AFRICOM_SOC_IR_Project_Setup_and_Workflow.txt`; `project_sources/DOC-03_DCOIR_Repository_Layout_Spec_v1_0_0.txt` |
| Official external sources | Not required for this page |
| Scope note | This legacy attachment explains the local harness concept. Use current GitHub and Airtable control surfaces for live workflow state. |

## Why these files exist

- The governed readable source for the PowerShell regression harness is `project_sources/run_DCOIR_Tests.ps1` when retained and current.
- The runtime filename for the current PowerShell harness is `run_DCOIR_Tests.ps1`.
- Older references to `run_DCOIR_Tests.cmd` or `run_DCOIR_Tests.cmd.txt` belong to legacy bundle history and are not current harness authority.
- The harness design assumes the collector runtime file sits beside the harness in the local runtime layout, while `DCOIR_Collector.zip` sits in `assets/` when a master restage ZIP is required.

## Harness parameters

| Parameter | Purpose | Current default |
| --- | --- | --- |
| `-Suite` | Select Core, Retrieval, QuickAliases, or FullRegression | Core |
| `-CollectorPath` | Path to the local runtime collector | `.\DCOIR_Collector.ps1` |
| `-OutputRoot` | Directory for test outputs | `.\TestResults` |
| `-MasterZipPath` | Path to the master collector ZIP used for restaging | `.\assets\DCOIR_Collector.zip` |
| `-ContinueOnError` | Keep running even if a step fails | off |
| `-SkipCleanup` | Leave DCOIR run output in place for inspection | off |

## Common local commands

| Use case | Command |
| --- | --- |
| Run the Core suite directly | `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\run_DCOIR_Tests.ps1 -Suite Core` |
| Run the Retrieval suite directly | `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\run_DCOIR_Tests.ps1 -Suite Retrieval` |
| Run the QuickAliases suite directly | `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\run_DCOIR_Tests.ps1 -Suite QuickAliases` |
| Run the full regression directly | `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\run_DCOIR_Tests.ps1 -Suite FullRegression` |

## What the harness actually does

- Restages the working `DCOIR_Collector.zip` from the master ZIP before suites that require restaging.
- Invokes the stable collector runtime with `-Quick` aliases rather than inventing separate test-only code paths.
- Writes logs and summary outputs under the run output root.
- Lets the operator leave artifacts in place with `-SkipCleanup` when needed for debugging.

> Supporting human-readable legacy attachment copy. Not part of the DCOIR control plane.
