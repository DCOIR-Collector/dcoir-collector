DCOIR Current-State And Version-Checking Enforcement Workflow Decision - 2026-04-04

Purpose
- Record the approved 2026-04-04 workflow decisions for current-state coordination, version-checking enforcement, and explicit governance-drift handling across the active DCOIR continuity surfaces.
- Turn the governance-gap concern into a real bounded enforcement model instead of leaving it as a chat-only observation.

Approved durable workflow decisions
- The active continuity surfaces in the initial enforcement set are:
  - `project_sources/CP-01_DCOIR_Version_Manifest.txt`
  - `project_sources/CP-02_DCOIR_Change_Log.txt`
  - `project_sources/LOG-03_DCOIR_Session_Handoff_Brief.txt`
  - `project_sources/LOG-05_DCOIR_Session_Resume_Anchor_2026-04-03.txt`
  - `project_sources/LOG-01_DCOIR_Todo_Log.txt`
  - `project_sources/LOG-01_DCOIR_Todo_Index.txt`
  - `project_sources/todo/01_Active_Now.txt`
- These initial active surfaces should carry the same shared `current_state_id` while they are aligned on one active work line.
- `CP-01` and `CP-02` remain the primary authority pair. The shared `current_state_id` is a coordination and drift-detection aid, not a replacement for the control plane.
- Before trusting a major resume, governed update, or session close-out that depends on current continuity state, compare the shared `current_state_id` across the active enforcement set and compare the `CP-01` / `CP-02` version pair.
- Version alignment between `CP-01` and `CP-02` is necessary but not sufficient. A mismatched shared `current_state_id` across the active enforcement set is governance drift even if the `CP-01` / `CP-02` version pair still matches.
- `dcoir-source-authority-auditor` is the first explicit drift gate for this workflow line. It may detect drift, stop or bound work, and report the exact mismatch, but it does not silently rewrite governed files.
- When drift is found, the workflow must produce an attention-worthy callout and the smallest correct grouped remediation set rather than quietly continuing as though the surfaces are aligned.
- Declared next-work-item alignment remains a required human review check even after the shared `current_state_id` rollout. The initial mechanical enforcement target is the shared `current_state_id` plus the `CP-01` / `CP-02` version pair.

Initial shared state id
- `current_state_id`: `2026-04-04-enforcement-v1`

Reference drift-check sequence
1. Re-anchor to Project Instructions, then `CP-01`, then `CP-02`.
2. Resolve the initial active enforcement set when those files are present in the workspace.
3. Compare the shared `current_state_id` across that set.
4. Compare the `CP-01` / `CP-02` version pair.
5. If both checks align, continue to the narrower source-authority or task-authority review.
6. If the workspace is partial but no contradiction is proven, continue only in a bounded way and say exactly which surfaces were unavailable.
7. If the shared `current_state_id` or the `CP-01` / `CP-02` version pair mismatches, stop and report the exact conflict.

Reference remediation sequence
1. Identify the smallest active-surface set that must be refreshed to restore alignment.
2. Re-read the current control plane before drafting updates.
3. Batch the already-known continuity refreshes into one grouped transaction when the chosen GitHub lane is safe.
4. Verify the shared `current_state_id` and `CP-01` / `CP-02` version pair after the grouped refresh.
5. Only then treat the active continuity surface set as aligned again.

Implementation posture
- Patch the explicit drift gate first before widening the same pattern to resume automation or other helper skills.
- Keep the initial stamped-surface set small and operator-understandable.
- Use the shared `current_state_id` as a coordination signal, not as proof that every semantic detail already matches.
- Continue to treat exported tracker artifacts and helper working-state files as continuity aids, not control-plane authority.
- Keep the GitHub Desktop lane and the in-chat lane as execution-lane choices, not as the governance rule itself.

Immediate validation targets
1. Confirm that the initial active-surface set carries the shared `current_state_id` consistently.
2. Confirm that `dcoir-source-authority-auditor` clears an aligned fixture.
3. Confirm that the same auditor hard-stops an intentionally drifted `current_state_id` fixture.
4. Confirm that the resulting conflict report is explicit enough that the operator can see the mismatch and the smallest grouped remediation set.
5. Decide whether `dcoir-session-resume` should consume the same drift gate directly after this bounded validation pass.

Best next move
- Run bounded regression on the initial `current_state_id` rollout and the upgraded `dcoir-source-authority-auditor` drift gate, then decide whether to widen the stamped surface set or bind the same gate directly into `dcoir-session-resume`.
