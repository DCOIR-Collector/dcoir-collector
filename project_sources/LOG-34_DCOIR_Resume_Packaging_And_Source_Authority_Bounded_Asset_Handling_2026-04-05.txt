DCOIR Resume Packaging And Source Authority Bounded Asset Handling - 2026-04-05

Current state id: 2026-04-05-resume-authority-bounded-assets-and-batched-prompts-v1

Purpose
- Record the bounded resume/authority alignment pass across `dcoir-session-resume` and `dcoir-source-authority-auditor`.

Decision summary
- Repair `dcoir-session-resume` so the skill packages cleanly with valid skill frontmatter and grouped follow-up prompt guidance that matches the current batched manual-update posture.
- Repair `dcoir-source-authority-auditor` so missing supporting assets no longer trigger a hard-stop conflict unless the current task explicitly depends on those assets as authority.
- Keep authoritative readable-source absence as a hard-stop conflict and preserve the bounded-versus-hard-stop distinction explicitly in both the audit rules and the deterministic audit script.

Why it matters
- The coordinated campaign found that `dcoir-session-resume` still lacked the normal skill frontmatter even though it is part of the mandatory first-turn bootstrap pair.
- The same review found a contradiction in `dcoir-source-authority-auditor`: the skill said supporting assets are not control-plane authority, but the deterministic audit script could still hard-stop when only a supporting asset was absent from the workspace.
- Those two issues sit on the same resume/authority bootstrap lane, so repairing them together reduces friction for current-state checks in partial or batched delivery workspaces.

Validation notes
- The updated `dcoir-session-resume` and `dcoir-source-authority-auditor` skill packages pass clean packaging validation.
- Representative deterministic audit tests confirm that missing authoritative readable sources still hard-stop, while missing supporting assets now produce a bounded outcome when the task does not depend on them as authority.
- The current continuity surfaces were refreshed in the same change set so the coordinated campaign now carries valid resume packaging and bounded authority-audit asset handling together.

Next immediate move
- Continue the coordinated campaign into the next workflow-state or authority-adjacent helper-skill pass while keeping compatible fixes for the next meaningful grouped manual update/install wave.
